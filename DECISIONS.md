# Smeldr — Decision Log

This document is the permanent record of every architectural decision made for Smeldr.
Each entry captures what was decided, why, what was rejected, and what consequences follow.

**Format:** decisions are immutable once locked. New decisions are appended.
Revisions to existing decisions require a new entry that supersedes the original.

**How to use this document:**
- Before implementing a feature, check if a relevant decision exists
- Before changing an interface, check what depends on it here
- When onboarding (human or AI), read this before touching code

## decisions/ file structure

| File | Contents | New entries? |
|------|----------|-------------|
| `DECISIONS.md` | Index only — this file | No |
| `decisions/recent.md` | Rolling working file (~20KB limit) | Yes — new decisions |
| `decisions/nondecisions.md` | Non-Decisions only | Yes — Non-Decisions directly |
| `decisions/core.md` | Archive: D1–D22, A19–A65, A87, A88–A95, A97–A101 | No |
| `decisions/phase2-archive.md` | Superseded archive (was phase2.md; content now in topic files) | No |
| `decisions/phase3-archive.md` | Archive: A102–A120 | No |
| `decisions/phase4-archive.md` | Archive: A121–A125 | No |
| `decisions/phase5-archive.md` | Archive: A126–A130 | No |
| `decisions/phase6-archive.md` | Archive: A131–A135 | No |
| `decisions/phase7-archive.md` | Archive: A136–A138 | No |
| `decisions/phase8-archive.md` | Archive: A139–A150 | No |
| `decisions/phase9-archive.md` | Archive: A151–A157 | No |
| `decisions/phase10-archive.md` | Archive: A158–A169 | No |
| `decisions/phase11-archive.md` | Archive: A170–A183 | No |
| `decisions/phase12-archive.md` | Archive: A184–A190 | No |
| `decisions/phase13-archive.md` | Archive: A191–A193 | No |
| `decisions/phase14-archive.md` | Archive: A194–A200 | No |
| `decisions/phase15-archive.md` | Archive: A201–A209 | No |
| `decisions/phase16-archive.md` | Archive: A210–A214 | No |
| `decisions/phase17-archive.md` | Archive: A215–A219 | No |
| `decisions/phase18-archive.md` | Archive: A220–A221 | No |
| `decisions/phase19-archive.md` | Archive: A222–A224 | No |
| `decisions/phase20-archive.md` | Archive: A225–A226 | No |
| `decisions/phase21-archive.md` | Archive: A227–A233, D33–D37, A234–A235 | No |
| `decisions/phase22-archive.md` | Archive: A236–A240, D38–D39 | No |
| `decisions/phase23-archive.md` | Archive: D40–D42, A241 | No |
| `decisions/phase24-archive.md` | Archive: D43–D46, A242–A247 | No |
| `decisions/phase25-archive.md` | Archive: D47, A248–A251 | No |
| `decisions/phase26-archive.md` | Archive: D48–D49, A252–A255 | No |
| `decisions/auth.md` | Archive: D25, A66, D26, A83 | No |
| `decisions/content-api.md` | Archive: D27, A67, A74, A75, A77 | No |
| `decisions/docs.md` | Archive: D28, A69–A72, A76, A84–A86 | No |
| `decisions/media.md` | Archive: A73, D31, A79 | No |
| `decisions/nav.md` | Archive: D29, D30, A82 | No |
| `decisions/storage.md` | Archive: A68, A78, A80, A81 | No |
| `decisions/[topic].md` | Topic files on architect instruction | Only when instructed |

**Archiving rule:** When `recent.md` reaches ~20KB, corepilot reports it at session start:
"recent.md is Xkb — ready for archiving." The architect decides groupings and topic file
names via NEXT.md. Corepilot never archives autonomously. Non-Decisions go to
`nondecisions.md` directly and do not count toward the limit.

---

## Decision index

### Recent — [decisions/recent.md](decisions/recent.md)

| # | Title | File |
|---|-------|------|
| A258 | T211/D51: `App.DrainEvalQueue` applied its status transition via a raw `UPDATE`, never through `SQLRepo.Save`/`notifyAfter` — zero `ProvenanceRecord` for an eval-queue-driven transition despite the row's status genuinely changing. Authority half already closed (A241/D42); this is the observability half only. `ActorKind: "job"`, `ActorID: "drain-eval-queue"` — a fixed descriptive string, not a `Run` (D38, a heavier claim/lease/worktree object for M3 coding sessions, category mismatch for a stateless periodic sweep); generalises directly to `SweepStructural` (T223) as the same pattern, answering T220's own question. Settles the *actor* question only — whether a sweep's own *run record* is a `Run` or something else remains open, for T223's own plan to settle. Scope argued, not bundled: provenance in; signal dispatch/cache invalidation/rebuild triggers out (would activate every human-publish subscriber for an automated transition with no operator decision behind it). Reuses `recordProvenance`'s existing fail-open discipline — a write failure never blocks the queue-row deletion (A241's "not re-queued" rule unweakened). `Surface: "trigger"` — the enum value existed with no producer until now. 4 new tests. No exported symbols changed. Coverage: 96.3%; `DrainEvalQueue` 100%. Patch bump, matching A241's own precedent. Level 2 amendment. | [recent.md](decisions/recent.md) |
| A259 | T239: `main`'s CI went red with no causing commit — `govulncheck` reported five Go stdlib vulnerabilities (GO-2026-6218, -6091, -6090, -6089, GO-2026-5972), all fixed in go1.26.6, time-triggered by the vuln DB updating rather than any commit. Fix: `toolchain go1.26.6` in `go.mod`, not a `go` line bump — the vulnerabilities are stdlib patches, nothing about what the language or a caller needs changed, only which binary compiles the code; bumping `go` itself would overstate the change and misrepresent the git history the day a real language-version bump happens. Applied to all seven owned repos (`core`, `mcp`, `cli`, `media`, `oauth`, `agent`, `social`) — `core`/`mcp` are the only two with a `govulncheck` CI step (both resolve via `actions/setup-go@v5`'s `go-version-file: go.mod`, confirmed by reading each `ci.yml`), but the other five carry the identical exposure with less visibility, not less risk. Durable note recorded in `common/agent/skills/smeldr.md`'s "Common gotchas" (not a per-repo README) since this recurs across every repo on the affected Go version. Docs/config only, no exported symbols, no consumer-observable behaviour change — no version bump, no tag, matching A245/A246's own precedent. Level 1 amendment. | [recent.md](decisions/recent.md) |
| A260 | T243: the audit trail was write-only — `ProvenanceRecord` written on seven lifecycle events, readable through no surface (no HTTP route, no MCP tool, `ProvenanceFilter` zero production callers). New `SubjectProvenance(ctx, db, store, subjectType, subjectID) ([]ProvenanceEntry, error)` — a plain Go function, no new HTTP/MCP surface per `provenance-visibility-brief.md` §4.7 ("no new surface"), composed directly by callers (cloud's Trace certificate) the same way `cloud/internal/read.BuildTraceReading` already reads core data. Item-scoped only — never sets `ProvenanceFilter.ActorID` (§4.1: actor never a query key). `ProvenanceEntry.Gated` applies §4.3: only a `RequiredRole`+`Strict` transition exposes `ActorKind`/`ActorID`/`Surface`/`Reason`; reuses `validateTransition`'s own gate check via new shared `resolveFlowID`/`lookupTransitionGate` (DRY, extracted not duplicated, existing `validateTransition` test suite unmodified). Fail-closed direction stated explicitly as the opposite of `validateTransition`'s own (withhold the actor vs. reject the transition). Added scope: `Surface` now populated end-to-end (`SignalEvent`/`afterHookMeta` gain the field, threaded through a new unexported `notifyAfter` parameter across all 14 call sites); `Reason` plumbing complete but honestly empty until T237 threads real values. New exported symbols (`ProvenanceEntry`, `SubjectProvenance`). MINOR bump. Coverage: 96.3%. Level 2 amendment. | [recent.md](decisions/recent.md) |
| D57 | Task is the ship-code pipeline; Goal is the generic backlog item. Task's own state flow (`backlog → active → waiting-plan → plan-reviewing → implementing → commit-reviewing → done`) is deliberately shaped for the ship-code dispatch cycle, not the general work-item type — the original design doc named this explicitly. `Goal`'s flow (`open → in-progress → done`/`parked`) already is the general type: no plan-review, no commit-review, no phase that presupposes a repo or a diff. Going forward: architect creates a `Task` only for work that ends in a plan-reviewed, commit-reviewed change to a repo; everything else — a design discussion, a decision-in-progress, an investigation — is a `Goal`. Found 2026-08-15 reviewing `T244` (a concluded six-turn discussion round) stuck at `backlog` with no honest path to `done`: reaching it would mean walking through states that would each misrepresent something that never happened. The stuck state was not a flow defect, it was the wrong type — `Goal` already had the right shape, documented for exactly this in the original self-hosting design doc's own type mapping; the distinction existed from the start and drifted out of practice, not out of the model. `T244` stays a historical `Task`, closed with a note rather than retyped, since other Tasks already reference it by number. `T238` is unaffected — a different edge case (the flow's missing "verified, no work needed" exit), tracked separately. Rejected: renaming Task's own states to generic terms — they are accurate for what Task is actually for; the fix was choosing the right type, not blurring the right one's own vocabulary. | [recent.md](decisions/recent.md) |
| D56 | `Standing` means the governed state, and the two state axes are named as two. A Managed Item has two independent axes: lifecycle (`Status`: draft/scheduled/published/archived, every item has one, answers "is this visible and served") and governed state (StateFlow, only types with a registered flow, answers "where is this in its own governed process"). They are orthogonal — a `Decision` may be `published` and `proposed` simultaneously and both facts are useful. `Standing` (cloud `internal/read/anchor.go`) means the **governed state**, and is empty for a type with no flow — never a fallback to lifecycle, which would reintroduce today's defect quietly by letting `archived` mean two things again. Held-open counting therefore skips flowless types, which is correct: an item with no governed process cannot be unresolved. The lifecycle axis is CMS heritage and stays; it answers a real question, it is simply not what `Standing` is for. `constitution/vocabulary.md` maps the constitutional "Lifecycle / State (Art VII)" to `Status` and does not mention governed states at all — the map was missing an axis, which is how one field came to carry both; architect closes that gap in the same pass. Settles the half T236's honesty fix deliberately left open. | [recent.md](decisions/recent.md) |
| D55 | A closed module may sell a domain, never a capability the model needs. Smeldr may offer closed, separately licensed modules provided they add a domain rather than withhold a capability an organization needs to own and operate its model. The test, applied before anything is built: could an organization own and operate its own operational model without this? If yes it may be closed; if no it belongs in core under AGPL. This is a **reading of the existing text, not an amendment**: `cloud-strategy.md` says Smeldr does not withhold a capability an organization *needs to own and operate its model*, which is a condition of necessity rather than breadth — a module not required for ownership was never inside the prohibition. Article IX supplies the usable ladder (capabilities strengthen the model; features realize capabilities; implementations realize features): a capability is load-bearing for the model, a domain is a subject area the model is applied to. Rejected: amending the constitution to permit withholding capabilities (breaks Article XII, and unnecessary given the reading — an amendment made speculatively for an unplanned revenue line would itself be the loophole); ruling the add-on model out entirely (forecloses a proven revenue line on a misreading that is ours). No such module is planned; this fixes the boundary while nobody has an interest in where it falls. | [recent.md](decisions/recent.md) |
| D54 | A cycle spanning two repos is two Tasks joined by `depends_on`, one per repo — never one Task covering both. Each runs its own full flow: own plan section, own commit review, own `done`; the upstream Task reaching `done` releases the downstream one, which is the release ordering that already exists (upstream tag → proxy verify → downstream pin bump → downstream tag). Either party opens the downstream Task the moment the plan establishes a second repo is in scope, not at dispatch — dispatch cannot know, as T235 proved by being dispatched `XS` and turning out to be one core method plus two mcp schemas plus a release sequence. Why: three times the second repo's Amendment went missing (A250 found weeks later, A252 at review, A257 at close-out), always the second, never the first — because a `Task` has one state, consumed by the first repo's work, so the second half arrives after the state that would have checked it. Rejected: extra states on the Task flow (bakes a two-repo assumption into a flow shared by every band; an n-repo cycle would need n states); a `repos:` field (**a field is not a gate** — the defect is that nothing checked, and a declared-but-unconsulted field repeats `TypePairs`/A236, still an open loose end two months later); an Amendment-per-repo rule alone (composes with this, but alone is a rule with no checkpoint, which is what produced the three misses). | [recent.md](decisions/recent.md) |
| D53 | Breaking changes stay inside v1 until the first external importer; v2 is reserved for a stability statement, not a feature milestone. Breaking changes to core's exported API are taken inside v1 with no major bump for as long as no external importer exists, and no compatibility twin is added to preserve a signature for a caller that does not exist. "No external importer" is a checked fact, not an assumption: the module proxy's own importers index is consulted at release time, folded into T217's clean-clone and pin-currency check, and the first module outside `smeldr.dev/*` appearing there expires this decision's first half. Evidence at the time of writing: five importers of `smeldr.dev/core`, all ours (`agent/flow`, `core/pgx`, `mcp`, `media`, `social`); the index only sees published modules, so a closed internal consumer would not appear. v2's real cost is not the 150 import rewrites but that `smeldr.dev/core/v2` becomes what every future doc and example says forever, plus two live majors to reason about — bought today for a population of zero. Mechanical prerequisite recorded: `site-dev`'s `vanityHandler`/`vanityHTML` declares the module path as `smeldr.dev/{mod}`, so a v2 tag resolves for nobody until that serves `smeldr.dev/core/v2` and smeldr.dev is redeployed. Rejected: v2 now (spends the one-time stability statement with no audience); returning to v0 (checked — the proxy holds v1 tags and resolution prefers the highest, so a v0 tag is inert); continuing to add compatibility twins (the trigger — six of them would have protected nobody, and A256's own note said a third would be accretion). Authorises the consolidation plan's single breaking pass. Any Amendment taking a break under this decision cites D53 explicitly. **Addendum 2026-08-14:** a breaking change inside v1 takes a **MINOR** bump with `BREAKING` stated plainly in the CHANGELOG entry and the Amendment — within v1, MINOR is the largest signal available and PATCH would be actively misleading. The version number cannot carry the truth inside v1, which was the price accepted when v2 was declined, so the truth goes in the text a person reads before upgrading. Honest limitation: a machine reading only version numbers gets no warning, tolerable only because every consumer is ours and their pins move deliberately. The addendum expires with the decision's first half the day the importer check finds an external module. | [recent.md](decisions/recent.md) |
| D52 | `investigates` targets a condition's subject, which is often an edge: declared `Task` → `Decision` **and** `Task` → `RelationEdge`, where D45 declared only the first. Follows from D51 — a condition is identified by its qualified subject, and that subject is an edge for two of three provenances (the invalidated edge for detected, the `contradicts` edge for asserted; the item only for scheduled, today only `Decision`). For a contradiction the old pair is worse than incomplete: pointing an investigation at one of the two `Decision`s **chooses a subject**, which is what `contradicts`' symmetry forbids and what D45 itself established by making the kind non-directional. Declares the two pairs real today rather than `Task` → anything — no other type has a gated state producing a scheduled condition, so naming them would be speculation. `recordAssertProvenance` already writes `SubjectType: "RelationEdge"` (`relations.go:359-364`), so this declares what the model can already express. Honest consequence: `TypePairs` is documentation-only and unenforced (A236), so nothing rejected the wrong edge and nothing accepts the right one differently — the value is that a declaration read as truth should not be false. Amends D45, which otherwise stands; recorded as its own Decision rather than buried in D51 so the change carries its own lineage. | [recent.md](decisions/recent.md) |
| D51 | A condition is a projection identified by its subject; a finding is a persisted, detector-owned record. Three statements: (1) conditions remain projections owning no state, confirming D46 and closing T221's original framing, which predates D46 and asks what D46 already answered; (2) a condition's identity is its qualified subject `(subject_type, subject_id)` — the invalidated `RelationEdge` (detected), the `contradicts` edge (asserted), the item itself (scheduled) — and **no record is created for a condition in any provenance**; (3) findings are persisted as a thin type named `Finding`, written only by detectors, with no `create_*`/`update_*` tools and no human-driven flow, answering D46's own open question. Identity was settled by deriving what identity is FOR (stable rendering, model linkage, addressing, dedup, history) and testing each against the scheduled provenance: all five covered by the item, none needing a new object — so T221's three convergences were one absent write (T211's) seen from three angles. The thin shape follows from D46 removing `acknowledged`/`dismissed`/`accepted-as-is` (human claims about resolution, which D46 forbids) and `delegated` (model work via `investigates`); a write surface would be the invitation to exactly that. Named `Finding`, not T126's and the July analysis's `Insight` — D46 established finding as the precise term, and `Insight` is already a **Cloud capability** in cloud-strategy.md, so one word would name a free AGPL primitive and a sold service at once (Article X). Rejected: conditions as persisted items (state in the surface's own subject matter, the §13.5 failure); a seventh orchestration type with full CRUDAP; a per-condition record for scheduled; deferring identity to empirical observation (proposed by architect, rejected by Peter as hedging — the question was answerable and was answered). Unblocks T211, T223, T126. | [recent.md](decisions/recent.md) |
| D50 | The Task flow is the protocol; Signals are not a chat channel: the ship-code pipeline runs on `Task` state transitions alone (`waiting-plan → plan-reviewing` *is* plan-ready, `plan-reviewing → implementing` *is* approved-start, `implementing → commit-reviewing` *is* commit-ready) — each transition already carries actor, timestamp, audit and a `Reason` field; plans stay files. Retired for the live channel: protocol-verb `Signal` records, the `sequence` counter, the `read` step, dispatch signals (a dispatch is the `Task`'s own creation). `Signal` is reserved for system-emitted notifications (D42's class). The flow renames `architect-task` → `agent-task` — generic behaviour under a role-specific name was the layering smell. Delivery: session-start queries + desktop doorbell for interactive sessions; T231 events + M3's relay for headless, deferred with M3. Why: the Signal protocol reified the file channel's own compensations (messages/sequences/receipts exist because files cannot push and hold no state) onto a system that already has state, ordering, audit and events — double bookkeeping with no rule for which record wins, Article IV's silent ambiguity. Rejected: keeping both records (drift by design); building the relay now for interactive use (doorbell suffices; relay serves headless); keeping the old flow name. Takes effect between cycles — T230 finishes under the old protocol. | [recent.md](decisions/recent.md) |
| D49 | Transition authority for compiled types lives on `App`, not on the `MCPModule` interface: new `App.TransitionItem(ctx, typeName, slug, toState)` performs flow-validated transitions for compiled and dynamic types alike — `resolveItemTable` + the identical `validateTransition` call the REST path already makes, persisting via the established raw status-UPDATE pattern; the dynamic branch delegates to `DynamicContentRepo` unchanged. Rejected: a new required `MCPTransition` method on the exported `MCPModule` interface (generic on `Module[T]`, typed rev-CAS preserved) — Go interface satisfaction has no default-method escape, so a required method breaks every non-`Module[T]` implementer, violating the API stability promise for a benefit (typed rev-CAS on a status-only write) the codebase's existing status-write paths already forgo deliberately. Boundary: the raw UPDATE's `rev` behaviour must be stated explicitly in the implementing Amendment's text, whichever it is — it borders T227's rev-echo territory. | [phase26-archive.md](decisions/phase26-archive.md) |
| D48 | A generated MCP tool's authority requirement is derived from its structure, never enumerated by hand: when `smeldr_tool_policies` has no row for a tool name, the required operation is derived from the tool's own verb prefix (`get`/`list` → `read`, `create` → `create`, `update` → `update`, `publish`/`schedule` → `publish`, `archive` → `archive`, `delete` → `delete`), **if and only if a real registered module backs the parsed type name** (`moduleForType`, or `moduleForAdminList` for list-plurals); otherwise denial stands unchanged. **An explicit policy row always wins over derivation.** Settles T224: `seedToolPolicies` is a hand-maintained list, the six orchestration types' generated tools had no rows, and `authoriseTool`'s fail-closed not-found branch denied every one of them for a token holding a real admin grant on the live instance — `create_signal` worked and `get_signal` did not, for no reason an operator could inspect; M0 step 7 blocked on it. Rejected: seeding rows per generated tool per deployment (correct only for what was enumerated at the time — D47's own class, and the list was already patched once this week, A242's grant tools); missing-row-means-allow (Article I fails outright); derivation inside core's `ToolPolicy` (`parseToolName` and the module registry are mcp-side knowledge core cannot reach — and the module-confirmation step is what keeps unknown names failing closed). Boundary: `manage`/`administer`/`define-*` have no generated verb form and are never derived — such tools keep explicit rows; `get_goal_context`/`list_type_tools` get explicit `read` rows in the same change. The derivation rule lands in governance-model.md §4 in the same commit — a requirement an operator cannot read about anywhere is silent authority. | [phase26-archive.md](decisions/phase26-archive.md) |
| D47 | A structural sweep gets a structural answer: for any compiled `Module` type, **alive means the row still exists** and no status is ever consulted — not `superseded`, `archived`, `expired`, or any future terminal state. Hard delete is the only "gone". Runtime-defined types keep `status = 'published'` unchanged. Grounded in a principle rather than a per-type table, so it survives a seventh type: **a status is semantic, existence is structural, and answering a structural question with a semantic test is a category error whatever status is chosen.** That also dissolves the one case core-implementer was least sure of (`Signal`'s `expired`) rather than accepting it as risk. Closes a live hazard: `App.SweepStructural`'s built-in checker queried only `smeldr_dynamic_content` and returned false when it found no row, so on an orchestration instance the first run would have invalidated the **entire lineage graph**. Documented in its own godoc, enforced nowhere. Also closes the sideways route — `resolveItemTable` falls back to the dynamic table when no dedicated one exists, making a compiled type with a missing table indistinguishable from a dynamic one — by requiring positive evidence of registration in `smeldr_content_type_schemas` and returning an **error rather than a verdict** when it is absent: a sweep that cannot tell must decline, never delete. Rejected: terminal status means not-alive (destroys history — a `supersedes` edge is the record of the supersession); a per-type table (correct today, wrong at type seven); leaving the fix opt-in (does not reach the consumer who does not know they are at risk). | [phase25-archive.md](decisions/phase25-archive.md) |
| D46 | A finding and a condition are different objects. A **finding** is what a detector produces (an observation about the model) and is persisted, deduplicated (detector type + subject ID + finding fingerprint) and stateful; it resolves when its **detector stops firing**, never on a human's claim to have fixed it. A **condition** is what requires a human (something settled can no longer be treated as settled, and a role-gated transition would resolve it) and is a projection owning no state. **Suppressing a still-firing finding is not a surface action; it is registering a `Decision`** — the act is "the model detects this and we have ruled it stands anyway", which is authority-bearing, audited and reversible by supersession, and which answers "why is this hidden" with a ratified statement rather than a button. The finding keeps firing; the detector is still right. This reconciles the 2026-07-06 analysis (findings must be persisted or Ignore means nothing) with `workspace.md` §13.5 (the surface owns no state and cannot be made to lie): both were correct about different objects, and the analysis was internally in tension on the same point. Only **one** of Workspace's three provenances is a finding — asserted and scheduled conditions are not detected by anything. Rejected: a dismiss action on the surface; the inverse assignment (stateful conditions, computed findings); and no suppression at all. Open: how a suppressing `Decision` links to the finding, and whether `Insight` (T126) is the persisted finding type. | [phase24-archive.md](decisions/phase24-archive.md) |
| D45 | Two relation kinds complete the Workspace model, both registered beside D36's four: `investigates` (Task→Decision, `asserted`, directional) carries delegation, so Workspace may originate model work without owning it and without naming a person; `contradicts` (Decision→Decision, `inferable`, **non-directional**, `weighted`) is the vehicle for semantic contradiction. Declaring `contradicts` does not make Smeldr detect anything — it gives reasoning performed elsewhere a declared shape to arrive in, with `edge_class` recording whether a human asserted it or an agent inferred it. Without it the "asserted" provenance of a Workspace condition (`design/workspace.md` §13.9e) had no vehicle and a third of the model was theoretical. First `inferable` kind and first non-directional kind in the system: contradiction is symmetric, so a direction would be a fact the model does not have. `Weighted: true` documents that confidence is meaningful; verified that `Weighted` is written and read back and consulted nowhere, the same documentation-only status `TypePairs` has per A236. Rejected: omitting `contradicts` entirely (confuses detecting with receiving), making it directional, and reusing `supersedes` (supersession is a decision someone made, contradiction is a claim about two things that both still stand — collapsing them destroys the distinction Workspace exists to surface). | [phase24-archive.md](decisions/phase24-archive.md) |
| D44 | An authority mutation always leaves a record, and the record is not optional: granting a role, revoking a grant, and defining or redefining a role are all recorded in `smeldr_governance_audit` with actor, action, target, before, after and time. Wiring governance wires audit — `App.Governance` creates the table and holds the store beside the `RoleStore`, so there is no option to turn it off and no path to a governance-enabled instance with no audit trail. Follows from D43 rather than standing alone: D43's argument is that granting is an act with an actor, a target and a time, and that argument depends entirely on the record existing, so making the record optional makes the decision optional. Rejected an opt-in `ServerOption` (proposed while planning T216, and reasonable — but what would be optional is not a subsystem, it is the evidence a decision happened) and wiring audit in `example/server` only (would fix the live instance and leave every other deployment to remember, which is the exact failure mode being fixed). Boundary stated so the two are never conflated: governance audit answers who was given the right to act, `ProvenanceRecord` answers who then acted; a gap in one is never argued away by pointing at the other. `RoleStore.WithAudit` keeps its per-request shape — the actor is attached per call, never wired once at startup. | [phase24-archive.md](decisions/phase24-archive.md) |
| D43 | Creating a token confers no governance role: `smeldr_tokens.role` and `smeldr_role_grants` are two authority models and nothing silently bridges them. `migrateTokenGrants` is removed rather than repaired, `RoleStore.Grant`/`ListGrants`/revoke get an MCP surface under `administer`, and A237's bootstrap grant stays as the one deliberate exception (an instance whose first token holds no grant is a locked door, not a governance model). Settles a real defect found on `process.smeldr.dev`: grants were keyed on `smeldr_tokens.id`, the SHA-256 fingerprint (`governance.go:329`), while `Authorized`/`RoleGranted` look up by the JWT's `User.ID` (`mcp/tool.go:89`), so every migrated grant was unreachable and only the bootstrap token ever worked. Diagnostic: a fingerprint is 64 hex chars, a `User.ID` is not. Rejected the two cheaper fixes (a `user_id` column; `Create` writing the grant) on Article I rather than on cost — a boot-time migration that turns a string match into a governance grant, with no actor and no audit record, is authority changing silently. Cost accepted deliberately: provisioning is now two acts. Opens a named gap, not closed here: `Revoke`'s last-admin guard counts `smeldr_tokens.role = 'admin'`, which after this is no longer the same as the last token granted admin. | [phase24-archive.md](decisions/phase24-archive.md) |
| D42 | Automation never crosses a role-gated transition: on reaching one it leaves the item where it is and records a `Signal` naming the role that must act. The predicate is `RequiredRole != ""`, independent of `Strict` and of whether governance is wired. The obvious implementation is wrong and was caught before it shipped: reusing `validateTransition` with an empty `actorID` passes on a *non-strict* gate (`state.go:440-445`), so a future ungated-but-role-naming transition would let a timer through. `Strict` answers a different question (interactive caller, real but unauthorized actor, governance unwired) and the two were conflated because only one had ever been asked. The `Signal` carries `receiver = <required_role>`, not a hardcoded `"admin"`. Rejected: automation-is-exempt (no authorization event exists on disk to point at) and trigger-carries-an-actor (Article XI — a timer firing months later manufactures a fresh authorization event out of a stale one). First applied in `App.DrainEvalQueue`; nothing in production changes today since the only target it reaches is D40's deliberately ungated `pending-re-evaluation`. | [phase23-archive.md](decisions/phase23-archive.md) |
| D41 | A conclusion that forecloses an alternative is registered as a Decision, or it gets rediscovered. Trigger, recognisable in the moment rather than in hindsight: you can name the alternative you rejected (every Decision D33-D40 has the form *X, not Y*, and if there is no Y nothing was decided); or you catch yourself citing it as settled with no D-number; or it cost multiple rounds or an external report to reach; or someone arriving without it would actively go the other way. Applies to conclusions from external sparring exactly as to internal ones. Not registered: implementation choices the code documents, anything derivable from source in five minutes, anything an existing Decision covers. Worked example: a 2026-07-06 analysis settled the free-detection boundary, findings-as-persisted-items, and digest-not-inbox; it lived in `analysis/` and a long `T111` row, neither read at session start, and was re-derived over a full day five weeks later with one part wrong the earlier work had already fixed. | [phase23-archive.md](decisions/phase23-archive.md) |
| D40 | Ratification and supersession after re-evaluation are authority-bearing: `pending-re-evaluation → ratified` and `pending-re-evaluation → superseded` get `RequiredRole: "admin"` + `Strict: true`, matching D34's two direct transitions. Entering `ratified` is the same act regardless of which door it comes through, and Article XI denies that having re-evaluated something is itself sufficient to make it authoritative. Entry *into* `pending-re-evaluation` stays ungated (observing that a premise moved is not an authority act), so the `schedule-eval` trigger is unaffected. Found while framing Workspace, decided as governance: the order is governance, then model, then surface, never the reverse. Does not close T211's raw-`UPDATE` bypass, which becomes more consequential as more transitions are gated. | [phase23-archive.md](decisions/phase23-archive.md) |
| D39 | Module paths follow publication, not ownership: `smeldr.dev/x` for public modules intended to be imported (requires a matching vanity entry in `smeldr/site-dev`, without which the path cannot resolve at all), `github.com/Smeldr/x` for private repos (resolves directly, no site change, no deploy — proven by `smeldr/cloud` depending on `github.com/Smeldr/mail`). A vanity path is infrastructure, not a name, and `smeldr.dev` is the public framework site, so advertising a private repo's location there is the wrong default. `smeldr/cloud` declares `smeldr.dev/cloud` with no vanity entry and stays that way: it is the commercial product, a leaf application nothing will ever import, so a path that cannot resolve costs nothing. It is the clearest evidence for the rule rather than an exception to it: the vanity path bought it nothing, and the only reason that stayed invisible is that it also cost nothing. No follow-up work. | [phase22-archive.md](decisions/phase22-archive.md) |
| D38 | Headless automation (M3) coordinates through `Run`, a sixth typed orchestration module: claims/leases reuse `SQLRepo.Save`'s rev-CAS (the only atomic test-and-set in the framework) rather than `Task` transitions or `DynamicTypeRepo`, which have no guard. Every lease-touching write must echo the `rev` it read or the CAS is satisfied by construction and degrades to last-write-wins silently. `Run` registers no `StateFlow`. Branches carry state, worktrees are disposable caches, names are never reused. Landing is serialized behind a land-time freshness gate, never auto-rebased; sequential numbers are allocated inside the land step. | [phase22-archive.md](decisions/phase22-archive.md) |
| D37 | Architect may propose, register, and commit new Decision entries directly to smeldr/core. Narrow exception to the standing git-write-actions rule; Peter ratifies each commit; D-number assignment follows the same read-index-first discipline as A-numbers. | [phase21-archive.md](decisions/phase21-archive.md) |
| D36 | Relation kinds for the orchestration graph: derives_from (Task→Goal), depends_on (Task→Task), ships_as (Task→Amendment, the only link back to a shipping Task), supersedes (Decision→Decision). implements (Amendment→Decision) stays an open, unconfirmed candidate, not registered. | [phase21-archive.md](decisions/phase21-archive.md) |
| D35 | Decision lineage: bounded, cycle-safe, read-time trace_lineage traversal — not a maintained transitive closure, not a new relation kind. Follows invalidated edges and flags them; whether it crosses into a superseded Decision's own supersedes chain is a separate, still-open question. | [phase21-archive.md](decisions/phase21-archive.md) |
| D34 | Decision authority: role-to-transition mapping via existing RoleStore/required_role, not a rank axis on Decision. EdgeClass (asserted/inferred/observed) stays pure epistemic origin, kept separate from authority. Scope-specific ratification uses a thin authorization wrapper with ScopeGlobal grants, not RoleGranted's ScopeDynamic. | [phase21-archive.md](decisions/phase21-archive.md) |
| D33 | Self-hosting roadmap ships in six sequenced milestones (M0–M5), not as one undertaking. M0 proves the dispatch pipeline only — never described as proof of the self-hosting vision itself, which needs M1 (authority) and M2 (lineage) on the critical path to M5 (supersede-triggered re-evaluation). | [phase21-archive.md](decisions/phase21-archive.md) |
| A235 | D35, M2: Build `trace_lineage` — `RelationStore.TraceLineage(ctx, anchorType, anchorID string, maxDepth int) (*LineageTrace, error)` (`lineage.go`, new), a bounded, read-time upstream traversal over `depends_on`/`derives_from` edges. Reuses `reachability.go`'s BFS shape (visited-set, tie-break) rather than `edges.go`'s `ChildrenOf` — a closer, previously-unnoticed precedent, flagged and approved before implementation — while adding the real batched-`IN(...)`-per-depth-level technique `reachability.go` itself lacks. Resolves D35's own open question: follows a superseded node to its replacement, recorded at the same Depth (a lateral step, not an extra hop), on the same reasoning D35 guard 3 already accepted for invalidated edges. Two real bugs found and fixed during implementation: a `Truncated` false-positive (frontier non-empty at maxDepth doesn't mean there's more; fixed with a one-time non-recording peek) and a supersede-chain resolution that only followed one hop instead of the current replacement (fixed by looping the chain walk, then capping it at `MaxLineageDepth` since chain length was otherwise unbounded). 20 new tests, 100% coverage on every new function. No MCP tool, no anchor-existence check (both explicitly out of scope, matching `Reachability`'s own precedent). Coverage: 96.2%. Level 2 amendment. | [phase21-archive.md](decisions/phase21-archive.md) |
| A236 | D36, M0 step 2: Register the four orchestration relation kinds — `RegisterOrchestrationRelationKinds(ctx context.Context, store *RelationStore) error` (`orchestration.go`, new), registering `derives_from` (Task→Goal), `depends_on` (Task→Task), `ships_as` (Task→Amendment), `supersedes` (Decision→Decision), each `Mode=asserted`/`Directional=true`/`Weighted=false`, per D36. D36 (commit `7db0eb6`) had already ratified these exact field values before this task began; NEXT.md's own framing presented them as an open design question, which the plan answered independently and converged on values identical to D36's — a citation miss caught and corrected before commit, not a re-litigated decision. Confirmed directly: `Mode` is validated but not enforced at write time (`edge_class` is chosen by which of `MCPAssertRelation`/`MCPProposeRelation`/`MCPObserveRelation` is called, writing `"asserted"`/`"inferred"`/`"observed"` respectively, independent of the kind's `Mode`); `TypePairs` is documentation-only, read only by `ValidateRelationKindDef`. Wired into `example/server/main.go`'s existing `ENABLE_RELATIONS`+`ENABLE_ORCHESTRATION` gate, exported so M0 step 3's permanent instance reuses the same function. 2 new tests in `orchestration_test.go` (round-trip + idempotency, one `UpsertKind`-failure path). Coverage: 96.2%, function 100%. MINOR bump v1.59.0 → v1.60.0. Level 2 amendment. | [phase22-archive.md](decisions/phase22-archive.md) |
| A237 | M0 step 3: fix a real D34 enforcement gap — the auto-created bootstrap admin token (`TokenStore.ensureBootstrap`, A83) couldn't ratify or supersede a `Decision`. `RoleStore.RoleGranted` (called by `validateTransition` for `Strict` transitions) queries `smeldr_role_grants` only, never `smeldr_tokens.role` — the bootstrap token's `role="admin"` field satisfied ordinary MCP tool gating but nothing governance-side. Traced deeper: the `actorID` `RoleGranted` checks against is the JWT's own `User.ID` (random, embedded at signing time), distinct from `smeldr_tokens.id` (the SHA-256 fingerprint) — two values never cross-referenced anywhere; no production code called `RoleStore.Grant` before this fix. New unexported `TokenStore.createToken(ctx, name, role, ttl) (raw, userID string, err error)` (`auth.go`) factors `Create`'s existing logic and surfaces the JWT's `User.ID`; `Create`'s exported signature/behaviour unchanged. `ensureBootstrap`'s signature (unexported) changes from no return value to `(userID string, created bool)`. `App.Handler()` (`smeldr.go`) grants the freshly created bootstrap token `admin` via `RoleStore.Grant` when governance is wired — fail-open, logged on error. A break-glass safety net, not a substitute for the architect's own later, deliberately-provisioned token. Rejected a setup script and a documented manual step — both lose to the idempotent-at-boot shape `ensureBootstrap`/`seedDefaultRoles` already established. 5 tests changed/added (`auth_test.go`, `coverage_test.go`); one named, accepted coverage gap (`createToken`'s `SignToken`-error branch, structurally unreachable, same as `encodeToken`'s own precedent). No new exported symbols. Coverage: 96.2% unchanged (`ensureBootstrap`/`Create` 100%, `createToken` 91.7%). Patch bump v1.60.0 → v1.60.1. Level 2 amendment. | [phase22-archive.md](decisions/phase22-archive.md) |
| A239 | D38 implementation (M3 part 1, smeldr/core): sixth orchestration content type, `Run` (`orchestration.go`), for mechanical episodes of headless automated work — one row per listening session from claim to merge/abandon. New typed module with fields: `TaskID`, `Repo`, `Machine`, `Branch`, `WorktreePath`, `BaseSHA`, `LeaseHolder`, `Outcome` (`RunOutcome` string-enum: `RunMerged`/`RunNeedsResync`/`RunStuck`/`RunFailed`/`RunOrphaned`, empty=in-flight), `Cleanup` (`RunCleanupState` string-enum: `RunCleanupPending`/`RunCleanupDone`), `AcknowledgedAt time.Time` (zero=not acknowledged). Deliberately no `StateFlow` — unlike five siblings, `Run`'s state lives in `LeaseHolder`/`Outcome` guarded by `SQLRepo.Save`'s rev-CAS, never via `validateTransition` (D38 names the wrong precedent to avoid: smeldr/agent's AgentJob registers StateFlow, looks natural for Run, is wrong). Every Run stays `Draft` forever, documented directly in `Run`'s godoc (not implicit; `MCPList` applies no draft-visibility filtering, confirmed at `module.go:2154`). Extended `CreateOrchestrationTables` and `RegisterOrchestrationTypes` in place — new `CREATE TABLE smeldr_runs` and new `app.Content(NewModule[*Run](...))` joined existing five, both functions' docs "five"→"six" — vs. building a parallel `CreateRunTable`/`RegisterRunType` pair. Registered `MCP(MCPRead, MCPWrite)` (required for the future M3 listener's `update`-path writes, not just consistency). `AcknowledgedAt time.Time` not `*time.Time` — verified before building, not assumed: `storage.go`'s `scanDest` only special-cases a field whose own type is `time.Time` (address naturally `*time.Time`); a `*time.Time` field addresses to `**time.Time`, unhandled, would fail scanning SQLite's string-formatted timestamps. Fixed to match `Decision.NextEvalAt`'s own established zero-value convention (`Node.ScheduledAt` is itself `*time.Time`, not a working precedent — caught during review; reproduced `scanDest`'s exact failure empirically against a real `SQLRepo`, a likely real pre-existing latent bug, flagged not fixed here). Rev-echo contract (D38 §3) documented on `Run`'s godoc, not tested — stated plainly that this cannot be tested at this layer since the future listener's write code doesn't exist yet; `TestRun_SaveRevConflict` instead proves `Run` genuinely participates in `SQLRepo.Save`'s real CAS. `TestRegisterOrchestrationTypes_flows` gained a load-bearing check: zero `smeldr_state_flows` rows for `type_name='Run'`, not just an unchanged count. `AGENTS.md`'s orchestration section corrected from "Four built-in types" (pre-existing staleness — `Goal`, added by A198, was never added either) to six, with a new warning against gating on `Run.status`. Companion mcp-repo prerequisite: A238. No exported symbols removed. Coverage: 96.2% unchanged. MINOR bump v1.60.1 → v1.61.0. Level 2 amendment. | [phase22-archive.md](decisions/phase22-archive.md) |
| A257 | T235, smeldr.dev/mcp half: `transition_item` and `set_content_status` both gain an optional `reason` string parameter, threaded to `App.TransitionItemWithReason`/`DynamicTypeRepo.SetStatusWithReason` (A256) respectively — satisfying a `Transition.RequiredReason` gate neither tool could reach before. `set_content_status` checked and fixed alongside `transition_item` per the dispatching task's own instruction to look for siblings with the same gap. Requires `smeldr.dev/core` v1.65.0+. New consumer-visible tool capability. MINOR bump v1.30.2 → v1.31.0. Backfilled at close-out review — third occurrence this session (after A250, A252) of the second repo's Amendment going missing in a two-repo cycle; flagged as structural by the architect, not fixed here. Level 2 amendment. | [recent.md](decisions/recent.md) |
| A256 | T235, smeldr/core half: new `App.TransitionItemWithReason(ctx, typeName, slug, toState, reason string) (map[string]any, error)` — mirrors `DynamicTypeRepo.SetStatus`/`SetStatusWithReason`'s exact shape (the same twin-method pattern, now used a second time). `TransitionItem` becomes a thin wrapper passing `""`, unchanged signature and behaviour, all 15 existing tests pass unmodified. Corrected two of the dispatch's own assumptions: `TransitionItem` did not already carry a reason, and no REST path (`updateHandler`/`MCPPublish`/`MCPSchedule`/`MCPArchive`/`newSetStatusHandler`) has ever threaded one either — `RequiredReason` has been unreachable from every entry point since A220, not a `transition_item`-specific gap. Out of scope, tracked separately as T237: the four `module.go` REST call sites still pass `""`. New exported symbol. Coverage: 96.3%; `TransitionItem` 100%, `TransitionItemWithReason` 97.4% (one already-named structurally-unreachable branch, A251's own precedent). MINOR bump. Level 2 amendment. | [recent.md](decisions/recent.md) |
| A255 | T232: decisions index/archive integrity sweep, two findings from T230's own investigation. `decisions/core.md` line 1274 had a mojibake (U+FFFD) where an em dash should be in Decision 16's own header — fixed. `A183`–`A190`'s index rows all linked to `[recent.md]` despite being archived months earlier (A250/A252's own defect class, older and wider) — verified individually against every archive file rather than trusting the reported range: A183 was archived a batch earlier than the other seven, into `phase11-archive.md`, not `phase12-archive.md` where A184–A190 actually are; a fix trusting the range alone would have written a second wrong link. Full mechanical sweep of all 114 index rows confirms these eight are the complete set — nothing else shares the defect. Discovered three distinct decision-header conventions across the project's history (`## D16`, the older `## Decision 16`, and `## Amendment A215`) while building the check; recorded so a future sweep doesn't rediscover it. Docs only, direct to `main`, no version bump. Level 1 amendment. | [phase26-archive.md](decisions/phase26-archive.md) |
| A254 | T230: `ERROR_HANDLING.md` documentation review, first since the project's first month. Four hand-listed findings, all independently verified against current source rather than patched blindly: the "single pipeline" claim is stale — `smeldr.dev/mcp`'s `errorFor` is a second, independent pipeline for JSON-RPC, distinguishing exactly five sentinels (`ValidationError`/`ErrBadRequest` → `-32602`, `ErrNotFound`/`ErrForbidden`/`ErrConflict` → `-32001`, `ErrRevConflict` → `-32002`), the rest falling to generic `-32603`; 9 of 12 sentinels were listed, missing `ErrRevConflict`/`ErrLastAdmin`/`ErrInternal`; the wrap example taught the retired `"forge:"` prefix (checked the whole repo for the same defect elsewhere — none found); the Decision 16 citation pointed at `DECISIONS.md` (index-only) instead of `decisions/core.md` where D16 actually lives, and the "Known gaps" table read as current rather than pure v1.0.1-era history. Two more findings beyond the hand-list, from re-reading the whole document: Tier 3's own description of which errors become generic 500s was imprecise (a `smeldr.Error` with `HTTPStatus() >= 500` gets the same treatment as an untyped error, not exempted by the interface); sentinel construction re-verified accurate, no change. All six fixed in `ERROR_HANDLING.md` alone. Not fixed, named instead: a mojibake character on `decisions/core.md`'s own Decision 16 header, and a wider/older version of the A250/A252 completeness gap (`A183`–`A190`'s index rows still point at `recent.md` despite being archived in July) — folded into a future follow-up task rather than expanding this cycle. No exported symbols, no behaviour change, no version bump. Level 1 amendment. | [phase26-archive.md](decisions/phase26-archive.md) |
| A253 | T227: `SQLRepo.Save` built a separate addressable copy for the DB write and never wrote the incremented `rev` — or even `UpdatedAt`/`CreatedAt` — back onto the caller's own struct; a successful `Save` left `item` untouched. Root cause of a symptom A226 had already fixed (`updateHandler`'s double-`Save`) without touching the contract itself, and the gap D38 §3 named as future work on `Run`'s own doc comment. Fix: `Save` appends `RETURNING "rev"` and uses `QueryRowContext` instead of `ExecContext` — the database-computed post-write `rev` comes back in the same round trip, no extra query; `sql.ErrNoRows` replaces `RowsAffected() == 0` for `ErrRevConflict` detection, same sentinel. `UpdatedAt`/`CreatedAt` write back unconditionally on success (already known in Go, no round trip needed) via new unexported `writeBackTimestamps`. Guarded by `src.CanSet()` — a value-type `T` degrades to a no-op, not a panic. First use of `RETURNING` in `smeldr/core`. No signature change. Two existing tests (`TestSQLRepo_Save_RevConflict`, `TestRun_SaveRevConflict`) previously passed *because of* the bug and were rewritten with a deliberately-held-stale separate copy. `Node.Rev`/`Run` doc comments each gained one clarifying sentence; D38 §3's own listener-payload concern stays separate and unclosed. 5 new tests, `Save` 100% covered. Coverage: 96.3%. Patch bump v1.64.0 → v1.64.1. Level 2 amendment. | [phase26-archive.md](decisions/phase26-archive.md) |
| A252 | D49, smeldr.dev/mcp half: `transition_item` now calls the new `smeldr.dev/core` `App.TransitionItem` (A251) instead of cleanly rejecting compiled types; `get_valid_transitions`/`list_items_by_state` resolve status via each module's own `MCPGet`/`MCPList`, no new core API needed. Role-gated transitions behave identically to the REST path. `errorFor` gains `smeldr.ErrBadRequest` → `-32602`, closing a pre-existing JSON-RPC mapping gap for `validateTransition`'s `RequiredReason` branch. Requires `smeldr.dev/core` v1.64.0+. No exported symbols changed. MINOR bump v1.30.1 → v1.30.2. Level 2 amendment. | [phase26-archive.md](decisions/phase26-archive.md) |
| A251 | D49, smeldr/core half: new `App.TransitionItem(ctx, typeName, slug, toState)` extends flow-validated state transitions to compiled types. `Kind == "content"` delegates unchanged to `DynamicContentRepo`/`SetStatus`; `Kind == "compiled"` resolves the table via `resolveItemTable`, runs the identical `validateTransition` call `updateHandler` already makes (same D34/D40 gate, not bypassed), then a raw status `UPDATE` mirroring `applyConflictPolicy`'s own `conflictSupersede` precedent. Chosen over a new required `MCPModule` interface method specifically to avoid breaking non-`Module[T]` implementers (D49's own rejected alternative). Rev behaviour stated explicitly per D49's boundary: the raw `UPDATE` does not advance `Node.Rev`, matching `SetStatus`/`conflictSupersede`'s own existing choice, not a new risk. Fires `fireAsyncTriggers` (A240) after a successful compiled update; does not fire `notifyAfter`/`AfterPublish`-class hooks for either branch, symmetric with existing dynamic-content behaviour. A compiled type with no backing table returns a distinct error rather than falling into the dynamic-content branch (D47's own ambiguity guard, applied here too). Unregistered-type error now wraps `ErrBadRequest` (new `errorFor` mapping, paired mcp Amendment A252) — closes a pre-existing gap where `validateTransition`'s own `RequiredReason` case had no JSON-RPC mapping either. 17 new tests, full error-path table. No exported symbols removed. Coverage: 96.3%; `TransitionItem` 94.7% (two structurally-unreachable branches named, not chased — `Kind` can't change between the registry check and `DynamicContentRepo`'s own identical check). MINOR bump v1.63.3 → v1.64.0. Level 2 amendment. | [phase25-archive.md](decisions/phase25-archive.md) |
| A250 | D48, T224, smeldr.dev/mcp half: `authoriseTool`'s `ToolPolicy`-not-found branch now derives the required operation from a missing tool's own verb prefix (`get`/`list` → `read`, `create` → `create`, `update` → `update`, `publish`/`schedule` → `publish`, `archive` → `archive`, `delete` → `delete`) via new `deriveToolPolicy(toolName)`, but only when a real registered module backs the parsed type name (`moduleForAdminList` for `list_*`'s plural form, `moduleForType` otherwise) — an unknown or misspelled tool name still fails closed. An explicit `smeldr_tool_policies` row always wins over derivation. `manage`/`administer`/`define-type`/`define-flow`/`define-relation-kind` have no generated verb form and are never derived, matching D48's own boundary. New `TestAuthoriseTool_PolicyCoverage_Enumerated` derives the full tool list from a real `tools/list` response (not hand-listed) and asserts every tool resolves through an explicit row or derivation — the acceptance criterion made permanent, not a one-time check. `TestAuthoriseTool_GetSignal_NoLongerForbidden` reproduces T224's own live symptom directly through `handleToolsCall`. Requires `smeldr.dev/core` v1.63.2+ (`TypeDescriptor.Kind` correctly distinguishes compiled types, A248) — go.mod bumped only after core's tags were proxy-verified, not go.work-local, per A243's own precedent; CI caught the ordering gap live (mcp's enumeration test failed against the real, then-untagged proxy before the bump landed) before any release shipped. No exported symbols changed (`deriveToolPolicy` is unexported). Coverage: 96.1%. Patch bump v1.30.0 → v1.30.1. Level 2 amendment. | [phase25-archive.md](decisions/phase25-archive.md) |
| A249 | D48, T224, smeldr/core half: `seedToolPolicies` gains `get_goal_context` → `"read"` and `list_type_tools` → `"read"`. Both are framework discovery tools, not module-generated — D48's mcp-side verb-derived fallback can't rescue a missing row for either, so they need an explicit seed like any other framework tool (D48's own boundary). Verified live on process.smeldr.dev before this fix: a real admin-granted token got `-32001 forbidden` on both, no policy row to inspect. `AGENTS.md` already documented `get_goal_context` as Author-gated (A199) — this fix makes the seeded policy match, not a new promise. New `TestRoleStore_ToolPolicy_OrchestrationDiscoveryTools`. No exported symbols changed. Coverage: 96.2%. Patch bump v1.63.2 → v1.63.3. Level 1 amendment. | [phase25-archive.md](decisions/phase25-archive.md) |
| A248 | `App.Content()` registered every compiled module's `TypeDescriptor` with `Kind: "content"`, identical to genuine runtime-defined dynamic types, causing `DynamicContentRepo`'s compiled-type guard to never fire; MCP tools `transition_item`, `get_valid_transitions`, `list_items_by_state` crashed with `-32603 internal error: no such table: smeldr_dynamic_content` when called on compiled types. Fix: `Kind: "compiled"` now as a third value in the registry (alongside `"block"` and `"content"`), chosen over reusing `"block"` because dynamic.go's HTTP handler and mcp's own `define_content_type` MCP tool both echo `desc.Kind` in responses — `"block"` would be coincidentally safe today only because `DefineContentType` always forces `Kind="content"` regardless of input, but structurally unsound. `TypeDescriptor.Kind` field comment updated to document the three values. Un-skipped `TestDynamicContentRepo_CompiledType_Rejected` (now registers real orchestration types via `RegisterOrchestrationTypes` and exercises the real public path). No exported symbols removed. Coverage: 96.2% package-wide. Patch bump v1.63.1 → v1.63.2. Level 2 amendment. | [phase25-archive.md](decisions/phase25-archive.md) |
| A247 | Implements D47 (see D47's own entry for the category-error principle, the sideways resolveItemTable-fallback hazard, and the three rejected alternatives — not restated here). New unexported defaultTargetChecker(db DB) TargetChecker (smeldr.go) replaces App.SweepStructural's inline default: for a compiled type (resolved via resolveItemTable, reused from D42), alive means the row exists; for a runtime-defined type, unchanged status='published', now gated on the target confirmed present in smeldr_content_type_schemas (kind='content') first — absent or unqueryable returns an error (SweepStructural treats as skipped, not stale) rather than inferring "not alive" from absence. Replaces App.SweepStructural's own default rather than an opt-in constructor or example/server-only wiring, so resolveItemTable's fallback design covers any compiled Module type automatically, not just the six built-in orchestration ones. 9 new tests, all driven through App.SweepStructural, never the checker directly: compiled-target alive/deleted, superseded and archived Decision both keep their relations, the compiled-table query-error path, both ways the registry lookup can fail, a genuine dynamic-content deletion regression. One existing test fixed (registered its content type properly) rather than weakened to keep passing against the new registry check. No exported symbols changed. Coverage: 96.3% package-wide, defaultTargetChecker 90.9% (two DB-failure leaves in the unchanged dynamic-content branch, named not silent). No scheduler wiring, no SweepStructural return-value change — confirmed out of scope. Patch bump. Level 1 amendment. | [phase24-archive.md](decisions/phase24-archive.md) |
| A246 | T217: example/blog, example/api, and example/docs declared `go` lines below core's root requirement (blog: 1.26.3; api, docs: 1.26.2 vs. core's 1.26.5) — Go's toolchain auto-switch ignores replaced targets' `go` lines, so building from a clean clone failed with "module ../.. requires go >= 1.26.5"; bumped all three to `go 1.26.5`. example/blog/go.mod dropped its relative replace `smeldr.dev/mcp => ../../../mcp` (a path outside any core-only clone, confirmed load-bearing — main.go calls mcp.New(app)) for a real pin, establishing the rule example/server already embodies: in-repo dependency gets a relative replace, out-of-repo dependency gets a real pin. **Scope grew mid-task on a production finding**: process.smeldr.dev's own `/_health` showed `mcp:1.28.0` post-A245-deploy — A245 replaced core only, so the M0-step-4 instance shipped v1.63.0 core against mcp v1.28.0, meaning grant_role/list_grants/revoke_grant/token_id (A243/A244, shipped in mcp v1.30.0) never reached it, silently: a stale MCP-tool pin loses tools without a build error, a different failure class from A245's compile-time one, and `GOWORK=off go build/vet/test ./...` passing (A245's own check) does not catch it. Checked, not assumed, each of example/server's five out-of-repo pins against what's actually published: `mcp` v1.28.0 (behind, bumped to v1.30.0 — reviewed intervening CHANGELOG entries, all additive or non-breaking for this consumer), `social` v0.9.2 (behind — v0.10.1 includes a genuine behavior change, an explicit `status` field on create that was previously silently ignored now takes effect — flagged, not bumped, reported for a separate decision), `agent`/`media`/`oauth` (all already current, no change). Rebuilt the release artifact after the mcp bump; verified end-to-end against a running instance that `tools/list` now includes `grant_role`/`list_grants`/`revoke_grant` and `create_token`'s response includes `token_id`. README's `cd core` line added (quickstart failed before reaching the go.mod question). Verified from real isolation throughout: cloned into a tempdir entirely outside the `Smeldr/` tree, `GOWORK=off go build ./...` in all four examples, all exit 0, error text checked alongside exit codes. Noted, not silent: `example/server`'s `smeldr.dev/social` pin is now knowingly behind (v0.9.2, latest v0.10.1) with the reason on record — an unrecorded stale pin is precisely how `mcp` reached v1.28.0. Side effect flagged on review, not deliberately made: `go mod tidy` rewrote `example/server`'s `core` require line from `v1.54.0` to `v1.63.0`, which happens to make `/_health`'s `"core"` field currently true (T219's own decoration-not-measurement symptom) without closing T219 — the line goes stale again at the next core release with nothing re-deriving it automatically. No `smeldr.dev/core` package content changed. No version bump, no tag. Names, does not close, the isolation-build gap: a build check proves compilation, a currency check proves the pin is current, and they don't overlap — T217 now covers both. Level 1 amendment. | [phase24-archive.md](decisions/phase24-archive.md) |
| A245 | example/server/go.mod gains `replace smeldr.dev/core => ../..`, matching example/blog/example/api/example/docs, which already carry it — example/server was the only one of the four that didn't. Found building the v1.63.0 release artifact from a clean tag checkout with GOWORK=off (no gitignored go.work to paper over it): the pinned smeldr.dev/core v1.54.0 predates App.ContextPacketHandler (A214, v1.55.0), App.Provenance/ProvenanceStore (A220, v1.57.0), and RegisterOrchestrationRelationKinds (A236, v1.60.0), all used by main.go — README's own `cd example/server && go run .` has been unable to build from a fresh clone for about a month, invisible because every real build in this project goes through go.work's local override. Fix is the sibling pattern, not a version bump (a pin goes stale again at the next release; a relative replace never does). The other five pinned dependencies (mcp/agent/media/oauth/social) are separate repos, can't be relatively replaced from a core-only clone, and were checked (not assumed) to still be current: GOWORK=off go build/vet/test ./... all pass with only the core replace added. No smeldr.dev/core package content changed (example/server is its own module) — no version bump, no tag. Separately flagged, not fixed here: example/blog/go.mod also carries `replace smeldr.dev/mcp => ../../../mcp`, a path outside the repo — README's primary quickstart may have the same class of problem, unverified against a real clean clone. Level 1 amendment. | [phase24-archive.md](decisions/phase24-archive.md) |
| A244 | D43 context: A243 shipped grant_role but left no way to obtain token_id from create_token — manual JWT decoding was required to bridge the two-act sequence. TokenStore.CreateWithID(ctx, name, role, ttl) thin wrapper returns (raw, userID, err), reusing existing ts.createToken logic with no signature change to Create; smeldr.dev/mcp's create_token tool adopts it and adds "token_id" to the response, named for continuity with grant_role's argument. AGENTS.md grant-tools section corrected. Existing TestGrantTools_EndToEnd_MintedGrantRatifiesDecision rewritten in place to read token_id from create_token response; new TestHandleTokenTool_CreateWithID_TokenIDMatchesJWT verifies token_id matches JWT. RoleGrant.ID and RoleGrant.TokenID route correctly — grant_role/list_grants/revoke_grant operator surface closes end to end. 2 new core tests (CreateWithID, execError), 1 mcp test rewritten. No symbols removed; new exported TokenStore.CreateWithID. Coverage: core 96.2%, mcp 96.3%. MINOR bumps: smeldr/core v1.62.0 → v1.63.0, mcp rides A243. Level 2 amendment. | [phase24-archive.md](decisions/phase24-archive.md) |
| A243 | D43 + D44/T216, smeldr.dev/mcp half: three new tools (`grant_role`, `list_grants`, `revoke_grant`) in grant_tools.go mirroring `tokenToolDefs`'s pattern — `grant_role` takes `token_id`, `role`, optional `scope_static` ([]string) and `scope_anchor_id`, calls `rs.WithAudit(ctx.User().ID, s.app.GovernanceAuditStore()).Grant()` unconditionally per A242's guarantee; `list_grants` accepts optional `token_id` filter; `revoke_grant` takes grant `id` by its own key (deliberate asymmetry: you grant a role, revoke a grant by its own identity), all three gate on `s.app.RoleStore() != nil` and require Admin via A242's "administer" ToolPolicy — `token_id` means JWT `User.ID` at signing time, never `smeldr_tokens.id` (fingerprint), explicit in tool descriptions and AGENTS.md. `go.mod` bumped smeldr.dev/core v1.58.4 → v1.62.0 only after A242 tagged/released/proxy-verified (not go.work-local); `GOWORK=off` builds clean. Two end-to-end tests: `TestGrantTools_EndToEnd_MintedGrantRatifiesDecision` (create_token → VerifyTokenString → grant_role from separate admin → PUT /decisions/{slug} against `RequiredRole: "admin"`, asserts 200/Status == "ratified"); `TestGrantTools_AuditRecordsWritten` (dispatch path, queries `smeldr_governance_audit` by actor_token_id/action/target_id, proves D44's audit trail). 16 new tests in grant_tools_test.go covering presence/absence, success/scope variants, type validation, unknown role (-32001), filtered/unfiltered list, revoke success/missing, forbidden cases. Coverage: 96.3%. MINOR bump v1.29.4 → v1.30.0. No exported smeldr.dev/core symbols changed; new exported smeldr.dev/mcp behaviour (three tools), AGENTS.md gains governance grant tools section. Level 2 amendment. | [phase24-archive.md](decisions/phase24-archive.md) |
| A242 | D43 + D44/T216, smeldr/core half: removes `migrateTokenGrants` — verified, not assumed, that every production `RoleGranted`/`Authorized` call site (`module.go` x2, `orchestration.go`, `state.go`, plus mcp's `authoriseTool`) resolves the actor from `ctx.User().ID`/JWT-sourced `actorID`, never `smeldr_tokens.id` (the fingerprint the removed migration wrote into `token_id`), closing a verification D43 itself had only checked partially. New `pruneInertTokenGrants(ctx, db) (removed int, err error)` — enforcement, not migration: runs on every `migrateGovernance` call forever, deleting any `smeldr_role_grants` row whose `token_id` appears in `smeldr_tokens.id`, by membership not by 64-hex-character shape (a real grant could coincidentally match that shape without ever matching by membership, so shape-based deletion would have taken it too); fail-open, logged every run including zero. `App.Governance` now unconditionally creates `smeldr_governance_audit` and constructs a `GovernanceAuditStore` (D44: audit is not optional) — new `App.GovernanceAuditStore()` accessor beside `App.RoleStore()`. `seedToolPolicies` gains `grant_role`/`list_grants`/`revoke_grant`, all `"administer"` — must land with `smeldr.dev/mcp`'s own tool implementation (A243) in the same release, since `authoriseTool` denies an unpolicied tool for everyone. `RoleStore.WithAudit` bakes one fixed actor into its returned store — cannot be wired once at startup, every caller derives a fresh copy per request/actor. 17 new/extended tests. No exported symbols removed; new `App.GovernanceAuditStore()`. Coverage: 96.3% (all touched functions 100%). MINOR bump v1.61.2 → v1.62.0. Level 2 amendment. | [phase24-archive.md](decisions/phase24-archive.md) |
| A241 | D40 + D42/T211's authority half (smeldr/core): Part 1 (D40) — `orchDecisionFlow()`'s `pending-re-evaluation→ratified`/`→superseded` gain `RequiredRole: "admin"`/`Strict: true`, matching D34's direct-path transitions exactly; `decisionRatifyModule` generalized to `decisionModuleAtStatus(t, status string)` (3 existing callers pass `"proposed"` explicitly, no behaviour change), reused for 4 new end-to-end 403/200 tests through the real `updateHandler` path. Part 2 (D42) — `App.DrainEvalQueue` now declines to cross any `RequiredRole`-gated transition itself: new `drainAuthorizationGate(ctx, db, table, typeName, itemID, toState string) (fromState, requiredRole string, err error)` reads the item's live status (`smeldr_eval_queue` has no `from_state` column, confirmed directly) then looks up whether the transition is role-gated, independent of `Strict`/governance-wiring — deliberately not a `validateTransition` reuse, which was checked and found to pass an empty `actorID` through on a non-strict gate (`state.go:440-445`), the exact gap D42 documents. New `recordAuthorizationRequiredSignal(ctx, db, typeName, itemID, fromState, toState, requiredRole string) error` records a `Signal` (`receiver=<the declared role, never hardcoded>`) instead of applying the transition; the queue row is still deleted, matching the existing not-re-queued rule. 15 new tests (6 direct `drainAuthorizationGate` unit tests covering every branch — including a genuine no-flow-at-all case caught needing `newSQLiteDB` instead of `newMigratedDB`, since the latter always seeds a `"default"` flow that would otherwise mask it; `recordAuthorizationRequiredSignal` success + error; `DrainEvalQueue`-level wiring tests through the real entry point only, per architect review criterion). No exported symbols changed. Coverage: 96.2% (`drainAuthorizationGate`/`recordAuthorizationRequiredSignal`/`DrainEvalQueue` all 100%). Patch bump v1.61.1 → v1.61.2. Level 2 amendment. | [phase23-archive.md](decisions/phase23-archive.md) |
| A240 | Async `TransitionTrigger` pipeline now fires for typed orchestration modules (smeldr/core): `fireAsyncTriggers` in `state.go` had exactly two call sites in `dynamic.go` (`DynamicTypeRepo.setStatus`, `DynamicTypeRepo.ScheduleContent`), zero in `module.go` where six built-in typed orchestration types live (`Signal`, `Task`, `Decision`, `Amendment`, `Goal`, `Run`). Real status transitions in `module.go` occur in five places: `updateHandler` (HTTP PUT, guarded with `if prevStatus != newStatus` to avoid a wasted DB query on a same-status edit), `MCPPublish`, `MCPSchedule`, `MCPArchive`, `processScheduled` (background scheduler for overdue items). Single line added at each site immediately after `m.repo.Save(...)`: `fireAsyncTriggers(ctx, m.db, m.contentTypeName, fromState, toState, itemID)`, with `MCPPublish`/`MCPSchedule`/`MCPArchive`/`processScheduled` calling unconditionally (mirrors `dynamic.go`'s own pattern), whereas `updateHandler` guards its call. `MCPUpdate` deliberately NOT touched — its own doc comment plus `module.go:2333` confirm it always restores `Status` to the pre-existing value, so it can never represent a real transition. Confirmed unintended, not a deliberate boundary: `Decision`'s own `schedule-eval` trigger on `proposed→ratified` (A187, 2026-07-01) had never fired for any ratified Decision in the ~5 weeks since it shipped. Scope narrowed on architect review from three findings to one: wiring `agent.NewEvalQueueScheduler` into `example/server` (the documented, tested drain mechanism, never called there) was **held**, not shipped — the architect traced that shipping it alongside this fix would mean the live `process.smeldr.dev` instance starts auto-transitioning ratified Decisions with zero Signal/Provenance/cache-invalidation observability (this task's own third finding: `DrainEvalQueue` applies its transition via a raw SQL `UPDATE`, never through `Save`/`notifyAfter`) **and** zero `validateTransition`/`RoleGranted` call (a fourth finding, from the architect's own independent review: `DrainEvalQueue`'s body calls neither). Both facets tracked together as **T211** — the `example/server` wiring itself is not part of T211's scope, it is a separate, un-numbered deferral T211 unblocks. 8 tests (7 new + the pre-existing, unaffected `TestSetStatus_firesAsyncTrigger`): each drives a real typed-module transition path (HTTP PUT, or a real `MCP*`/`processScheduled` call), never `fireAsyncTriggers` directly, per the architect's own explicit review criterion — including `TestModule_updateHandler_noTriggerOnSameStatus` and `TestModule_MCPPublish_firesAsyncTriggerEvenOnSameStatus`, which pin the guarded-vs-unconditional asymmetry by registering a trigger ON the exact same-status transition itself (not a different one), independently verified by temporarily removing each guard/call and confirming the corresponding test failed. New shared helper `setupTriggerFlowTransition` (parameterized `from`/`to`). No new error-path table — five call sites to an existing, already-tested, void function. No exported symbols changed. Coverage: 96.2% unchanged. Patch bump v1.61.0 → v1.61.1. Level 2 amendment. | [phase22-archive.md](decisions/phase22-archive.md) |
| A238 | D38 prerequisite (M3 part 1, smeldr.dev/mcp): `ErrRevConflict` gets its own JSON-RPC code, `-32002`. Before: `errorFor` (`tool.go:101-116`) branched explicitly on `ValidationError` (-32602) and `ErrNotFound`/`ErrForbidden`/`ErrConflict` (-32001), falling through to `-32603` "internal error" for everything else including `ErrRevConflict`. Since `ErrConflict` and `ErrRevConflict` (smeldr/core `errors.go:60,65`) are two independently-constructed sentinels, `errors.Is(err, smeldr.ErrConflict)` can never match an `ErrRevConflict`-carrying error, making the two indistinguishable at `-32603` — the same bucket a transient `SQLITE_BUSY` reaches. D38's fencing protocol requires a listener to reliably tell "lost the lease race, re-read `lease_holder`" from "transient, retry" by code alone, not by parsing message text — reusing the existing `-32001` bucket (shared with NotFound/Forbidden/Conflict) wouldn't have fixed that either, just moved the ambiguity. Fix: `ErrRevConflict` gets its own `-32002` branch, added directly after `ErrConflict`. `errorFor`'s own doc comment, previously undocumented for its `ErrConflict` branch too, updated to list all five outcomes explicitly. Checked all twelve sentinels in `errors.go` systematically; none besides `ErrRevConflict` are named or implied anywhere in D38 as needing the same treatment. New test `TestErrorFor_RevConflict` (`coverage_gap_test.go`), matching the existing bare-sentinel test pattern. Coverage: 96.4% unchanged. No exported symbols changed. Four pre-existing golangci-lint findings (`mcp_test.go`, `node_tools.go`), confirmed identical on `main` before this work, flagged not fixed — unrelated files. Patch bump v1.29.3 → v1.29.4. Level 1 amendment. | [phase22-archive.md](decisions/phase22-archive.md) |
| A201 | T120: validTransitionsFor flowID int64 → string in smeldr.dev/mcp (v1.27.1). smeldr_state_flows.id is TEXT NOT NULL PRIMARY KEY (UUID) since A195/core v1.52.2; scanning into int64 caused sql.Scan to fail → empty transitions list. Fix: var flowID string. Also adds .github/workflows/ci.yml to mcp repo (build, vet, test, BOM check, race, staticcheck, govulncheck; no Postgres job). Fixes TestStateTool_GetValidTransitions_HappyPath and CustomFlow. mcp v1.27.1. | [phase15-archive.md](decisions/phase15-archive.md) |
| A203 | T124: wire ENABLE_ORCHESTRATION toggle into example/server — CreateOrchestrationTables + RegisterOrchestrationTypes; AGENTS.md env-var table updated. No core API change, no version bump. | [phase15-archive.md](decisions/phase15-archive.md) |
| A204 | T125: example/server buildApp refactor — extract ServerConfig (24 fields), ServerResult (App, MCP, TokenStore, StopAll), parseConfig, and buildApp(cfg, db) (ServerResult, error) from monolithic main(). buildApp has no log.Fatalf; all subsystem failures return error. main_test.go (TestServerToggles, 7 sub-cases) and preflight_test.go (//go:build preflight, binary-spawn) written. go.mod bumped: core v1.52.2→v1.54.0, mcp v1.26.1→v1.28.0. Level 2 amendment. | [phase15-archive.md](decisions/phase15-archive.md) |
| A205 | T125: bug fix — RegisterOrchestrationTypes table derivation — fixed explicit Table() options on all 5 orchestration repos (smeldr_goals, smeldr_signals, smeldr_tasks, smeldr_decisions, smeldr_amendments). Pre-existing bug uncovered by T125 test suite. Level 1 amendment. | [phase15-archive.md](decisions/phase15-archive.md) |
| A206 | T125: bug fix — App.Relations() missing CreateSchemaTable call — Relations() wires syncSaveHook querying smeldr_content_type_schemas, but table was absent when ENABLE_RELATIONS=true without ENABLE_DYNAMIC_CONTENT. Fix: call CreateSchemaTable(a.cfg.DB) inside Relations() (idempotent, guarded for nil DB). Level 1 amendment. | [phase15-archive.md](decisions/phase15-archive.md) |
| A207 | T127: list_type_tools discoverability meta-tool in smeldr.dev/mcp. Takes type_name (snake_case) and returns all MCP tool names for that compiled content type. Derived live from mcpToolDefs+mcpAdminReadToolDefs — cannot drift. Fixes ChatGPT incident (smeldr.dev, 2026-07-06): found update_essay but concluded list_essays/get_essay didn't exist. mcp v1.29.0. Level 1 amendment. | [phase15-archive.md](decisions/phase15-archive.md) |
| A208 | T130: fix staticcheck CI findings in smeldr.dev/mcp (3 findings: U1000 unused helper in relation_tools_test.go, SA5011 nil-guard violations in relation_tools_test.go:156 and redirect_tools_test.go:211,227). Test-file-only changes. Level 1 amendment. | [phase15-archive.md](decisions/phase15-archive.md) |
| A209 | T132: Move `discoverToolDef()` append to first position in `handleToolsList()` to ensure core tools survive client-side tail truncation (level 1 amendment, smeldr.dev/mcp). | [phase15-archive.md](decisions/phase15-archive.md) |
| A210 | T133: mcp v1.29.1 patch tag (T130+T132 closure) + CLAUDE.md Level 1 classification clarification — standalone-module patch tags required for consumer-observable behaviour changes. | [phase16-archive.md](decisions/phase16-archive.md) |
| A211 | T137: Social REST API (smeldr.dev/social v0.10.0) adds 5 CRUD endpoints with Bearer token auth; CLI REST switch (smeldr.dev/cli v0.15.2) converts 7 post commands from MCP to REST requests. Level 2 amendment. | [phase16-archive.md](decisions/phase16-archive.md) |
| A212 | T136: SQLRepo `rev` column auto-migration (core v1.54.2) — `Module[T].setDB()` invokes `MigrateNodeRevColumn()` via unexported `tableName()` interface with nil-DB guard. Closes production gap from 2026-07-07 emergency patch. Level 2 amendment. | [phase16-archive.md](decisions/phase16-archive.md) |
| A213 | T138: Go 1.26.5 toolchain bump (GO-2026-5856 crypto/tls ECH privacy leak) — 7 repos (core, mcp, cli, agent, social, media, oauth): go.mod `go` directive 1.26.4→1.26.5 (media/oauth 1.26.3→1.26.5). Fixes govulncheck red on core/mcp CI. No API changes, no version bumps, no tags. Level 1 amendment. | [phase16-archive.md](decisions/phase16-archive.md) |
| A215 | T147 Part 1: Wire App.ContextPacketHandler into example/server — new INSTANCE_NAME env var (ServerConfig.InstanceName, default "smeldr-dogfood"); two EnableRelations blocks consolidated into one hoisted var rs block; ContextPacketHandler gated on both ENABLE_RELATIONS and ENABLE_ORCHESTRATION; TestServerToggles 7→9 sub-cases. No core exported symbols changed. Level 1 amendment. | [phase17-archive.md](decisions/phase17-archive.md) |
| A214 | T145: Context Packet v1 — HTTP endpoint `GET /packet/{type}/{slug}[?depth=]` for bounded orchestration context. Exports 7 types (ContextPacket, PacketSource, PacketAnchor, PacketBoundary, PacketOmission, PacketItem, PacketRelation) + BuildContextPacket + App.ContextPacketHandler. Depth 1–2, per-type cap 25, diamond-safe traversal. v1.55.0. Level 2 amendment. | [phase16-archive.md](decisions/phase16-archive.md) |
| A216 | T148: Orchestration create-time state validation — `validateInitialState(ctx, DB, typeName, statusName)` rejects any status string not registered in the type's own flow at create time (both `createHandler` HTTP POST and `MCPCreate` MCP paths). `validateTransition` gains a target-state pre-check that returns a specific "not a valid target state" error before falling through to the transition-edge lookup. Fail-open on nil DB, non-SQLite, missing flow, and structural query errors. 10 new tests (8 unit + 2 module integration). No exported symbols changed. Level 1 amendment. | [phase17-archive.md](decisions/phase17-archive.md) |
| A217 | T150: updateHandler state governance + published→draft default transition — `validateTransition` is now called in `updateHandler` when `prevStatus != newStatus`, enforcing RequiredRole gates and state-flow correctness on HTTP PUT /{prefix}/{slug}. Fail-open semantics (nil DB, no flow, structural errors) preserved. Also adds `{"published","draft"}` to the default flow's transition list — a genuine independent gap: the unpublish path was only reachable in production via nil-DB fail-open. `TestModule_updateHandler_unpublish` updated to use a real migrated SQLite DB. 4 new/updated tests. No exported symbols changed. Level 2 amendment (route behaviour change, two files). | [phase17-archive.md](decisions/phase17-archive.md) |
| A218 | Agent role rename: pilot → core-implementer — `CLAUDE.md` (12 lines) and shared `skills/smeldr.md` role wording updated; `smeldr/architect/context/corepilot.md` renamed to `context/core-implementer.md` (git mv), incl. fixing a known media-version typo and two version-table rows (cli, oauth) found wrong during startup-test verification. Scope addition: reconciled all four `CLAUDE.md` passages describing commit approval with its own Signal protocol (commit-ready/commit-approved), removing a chat-only "yes" framing and a push-permission self-contradiction. No exported symbols, no behaviour change. Level 1 amendment. | [phase17-archive.md](decisions/phase17-archive.md) |
| A219 | T153: Reachability as a general platform primitive — `reachability.go` (new): `RelationStore.Reachability(ctx, anchorType, anchorID, kind, direction string, maxDepth int) (*Reachability, error)`; `ReachabilityItem`/`ReachabilityRing`/`Reachability` exported types; `MaxReachabilityDepth = 10`. Bounded BFS over the relation graph, general-purpose (any anchor type), one ring per hop, absence reported explicitly. Built standalone, not as a `ScopeDynamic` extension (shape mismatch + governance risk isolation). 15 tests, 100% coverage on new functions. Level 2 amendment. | [phase17-archive.md](decisions/phase17-archive.md) |
| A220 | T149: Transition provenance — `provenance.go` (new): `ProvenanceRecord`/`ProvenanceFilter` types, `ProvenanceStore` interface, `NewProvenanceStore(DB)`, `CreateProvenanceTable(DB)`, `App.Provenance(store) *App` (subscribes to 7 completed-transition signals via the existing bus, fail-open). Purely additive — `AuditRecord`/`App.Audit`/`GET /_audit` unchanged (rejects design doc's original "replaces AuditRecord" framing as a v1.0.0 stability-promise violation); the 4 events both cover now double-record deliberately. `relations.go`: `insertEdge` records relation-assertion provenance via a `ctx.(Context)` type assertion — no signature change to `Assert`/`MCPAssertRelation`/`MCPProposeRelation`, no mcp-repo change. `state.go`: `Transition.RequiredReason bool`; `validateTransition` gains unexported `reason` param + fail-closed check. `migrate.go`: idempotent `required_reason` column migration. `dynamic.go`: new additive `SetStatusWithReason` (SetStatus unchanged). Known gap: MCP `transition_item` still can't supply a reason (flagged, not silently dropped). 30+ new tests, coverage 96.1%. Level 2 amendment. | [phase18-archive.md](decisions/phase18-archive.md) |
| A221 | T149 hotfix: `required_reason` missing from the `smeldr_transitions` CREATE TABLE statement in `migrate.go` — only added via the SQLite-only ALTER-TABLE migration, so a fresh Postgres install (`smeldr.New` against a real Postgres DB, no pre-existing `smeldr_transitions` table) never got the column, breaking `RegisterFlow` with "column required_reason does not exist". Caught by CI's `pgx integration` job on the A220 push. Fix: add `required_reason BOOLEAN NOT NULL DEFAULT FALSE` directly to the CREATE TABLE statement (same pattern already used for `active_state`/`conflict_policy` on `smeldr_state_flows`); `migrateTransitionReasonColumn` retained unchanged in role — now documented as upgrading pre-existing SQLite databases only, since fresh installs on any engine get the column from CREATE TABLE. No exported symbols, no test changes needed (existing test comments already assumed this exact behaviour). Level 1 amendment. | [phase18-archive.md](decisions/phase18-archive.md) |
| A222 | Webhook/outbound TIMESTAMPTZ scan bug — `webhook.go`'s `List`/`EndpointsForEvent` and `outbound.go`'s `fetchDueJobs` (core delivery-loop poll)/`ListJobsForEndpoint`/`ListDeliveryLogs`/`DeliveryStats` scanned `TIMESTAMPTZ`-documented columns directly into `time.Time`, hard-failing against any DB following the documented DDL (`modernc.org/sqlite` only auto-converts `DATE`/`DATETIME`/`TIMESTAMP`). Production webhook delivery was non-functional; masked because every test table (`webhook_test.go`/`outbound_test.go`/`integration_full_test.go` G26–G29) used `DATETIME` instead. Fix: `scanDest()`/`timeScanner` (A200) reused at all 5 non-nullable sites; `DeliveryStats`'s nullable case reuses `timeScanner.Scan` directly (a bare `TokenStore`-style single-RFC3339 parse was tried and empirically failed against the real driver-stored string format, confirmed via a debug test). Root cause of the malformed string: `realClock.Now()`/`webhookDispatch`'s `time.Now()` returned monotonic-bearing local time instead of `.UTC()` — fixed at the source. Test DDL and `datetime('now')`/`newFakeClock(time.Now())` fixture patterns corrected across 3 test files to match real production writes. New regression test proves the DeliveryStats round trip rather than assuming it. No exported symbols changed. Coverage: 96.1%. Level 2 amendment. | [phase19-archive.md](decisions/phase19-archive.md) |
| A223 | T176: Finish the Forge→Smeldr rename — `core/pgx` submodule (`package forgepgx` → `package pgx`, matching import path convention; `pgx/v0.2.0` tagged, breaking-minor per A129 precedent; `pgx/CHANGELOG.md` added) plus a full prose/log-prefix sweep across ~30 files, found during an awesome-go submission review. Six real bugs fixed alongside the branding cleanup: `doc.go`'s broken `[Signal]`→`[LifecycleEvent]` godoc cross-reference (stale since A183); `ai.go`'s dead-function godoc example and the actual `/llms-full.txt` "Generated by Forge" runtime output string; `example/docs/main.go`'s struct-tag sample using the dead `forge:` key instead of `smeldr:` (A107) — silently produced zero validation if copy-pasted; a stale `smeldr_test.go` comment about a nonexistent `"forge"` health-check key; four `WARN forge:` log-prefix inconsistencies. Preserved: `migrate.go`'s legacy `forge_*` table-rename literals, the `X-Forge-*` HTTP headers (T86/T87 deprecation window), `mcp.WithForgeFallback()`. No exported symbols changed; patch bump (consumer-observable output/log-string changes). Coverage: 96.1%. Level 2 amendment. | [phase19-archive.md](decisions/phase19-archive.md) |
| A224 | T178: Wire `App.Provenance()` into a real instance; fix `ActorKind` for human/job/agent — Part 1: `example/server`'s new `ENABLE_PROVENANCE`/`ServerConfig.EnableProvenance`, same convention as `ENABLE_RELATIONS`. Part 2: new exported `Job`/`Agent` `Role` constants (`roles.go`, deliberately unregistered in the permission hierarchy — classification tags via `IsRole`, not `HasRole`); `SignalEvent.ActorRoles []Role` (full role set, captured synchronously in `buildSignalEvent`); `actorKindFor(actorID, roles []Role)` resolves "job"/"agent"; `App.Provenance()`'s handler reads `ev.ActorRoles` directly (an originally-proposed ctx-recovery design was implemented, tested, and empirically confirmed broken — `dispatchBus`'s `context.WithoutCancel`/`WithTimeout` wrapping strips `smeldr.Context`'s method set — before this redesign); `relations.go`'s `recordAssertProvenance` now also role-aware, `CreatedByJob`'s override unchanged. Part 3 (independent fix): `App.Handler()` now also calls `wireSignalBus()` (previously only `App.Run()` did) — closes a real, previously-silent gap where any caller embedding Smeldr's `http.Handler` directly (a documented pattern) never got `OnSignal` subscribers wired at all; deliberately re-entrant, not run-once, since `Content()` can add modules after an early `Handler()` call. One incidental pre-existing bug found and flagged separately, not fixed here: `MCPCreate` hardcodes status `"draft"`, conflicting with every orchestration type's own non-`"draft"` `IsInitial` state. New exported symbols: `Job`, `Agent`, `SignalEvent.ActorRoles`. Coverage: 96.1%. Level 2 amendment. v1.57.3 → v1.58.0. | [phase19-archive.md](decisions/phase19-archive.md) |
| A225 | T180: Fix `MCPCreate`'s hardcoded `Draft` default breaking all 5 orchestration types via MCP (A224's own incidental finding, dispatched separately). `MCPCreate` forced an omitted status to the literal `Draft` constant, then `validateInitialState` (A216) correctly rejected it since `Draft` ("draft") is never a registered state for `Signal`/`Task`/`Decision`/`Amendment`/`Goal`'s own flows. Checked the equivalent HTTP path (`createHandler`) directly rather than assuming — it has no status-defaulting at all, silently persisting the literal empty string `""` on an omitted status, worse than MCPCreate's failure since `""` isn't a valid state or transition `from_state` in a custom flow, leaving the item permanently stuck with no error. Fix: new `defaultInitialState(ctx, db, typeName) string` (`state.go`) looks up the type's own registered `IsInitial` state via `smeldr_states.is_initial`, fail-open, deliberately skipping the default-flow fallback (its own initial state is always `"draft"` anyway); new `applyDefaultStatus` (`module.go`) shared by both `createHandler` and `MCPCreate`. Rejected a module-registration default-status hook as a second, driftable source of truth. No exported symbols changed. `example/server`'s `TestServerToggles/on/orchestration`/`on/orchestrationWithRelations` (previously failing on `main`) now pass; A224's own `on/provenance` test workaround removed as a third proof angle. Coverage: 96.1%. Level 2 amendment. v1.58.0 → v1.58.1. | [phase20-archive.md](decisions/phase20-archive.md) |
| A228 | T183: `smeldr.dev/mcp` — corrected `Handler()`/`Register()`/`messageHandler` doc comments claiming `POST /mcp` is a "2025-11-25 streamable HTTP" endpoint; it is registered to the same handler as `POST /mcp/message` (2024-11-05 SSE-transport relay), an alias not a distinct transport. Pure doc-comment fix, no exported symbol/behaviour/version change. Incidentally reproduced a pre-existing, unrelated test failure (`TestStateTool_TransitionItem_InvalidTransition`), spawned separately as T184. Level 1 amendment. | [phase21-archive.md](decisions/phase21-archive.md) |
| A229 | T181: Fix `PublishedAt` never stamped when created directly with status `"published"` via `createHandler`/`MCPCreate`. Empirically refuted NEXT.md's suspected second bug (a follow-up `publish_post` call being rejected by `validateTransition`) — `fromStatus == toStatus` short-circuits the check, and `MCPPublish` already unconditionally restamps `PublishedAt`, so no second bug exists. Fix: new `nodePublishedAtOf`/`stampPublishedAt` (`module.go`), IsZero-guarded (preserves an explicit caller-supplied `PublishedAt`), called from both create paths; deliberately not reused at the 4 existing transition-time stamping call sites (would silently break republish). No exported symbols changed. Coverage: 96.1%. Level 1 amendment. v1.58.2 → v1.58.3. | [phase21-archive.md](decisions/phase21-archive.md) |
| A230 | T184: `smeldr.dev/mcp` — fixed a stale assertion in `TestStateTool_TransitionItem_InvalidTransition` (`published → draft` rejected). Root-caused to core's Amendment A217 (2026-07-15), which deliberately added `published → draft` as an "unpublish" feature — `smeldr.dev/mcp`'s own `go.mod` pins core v1.54.0, predating that change, so real CI (no local `go.work`) was still green; only the gitignored local workspace (pointing at core's live tree) surfaced the staleness. Swapped the attempted invalid target to `"scheduled"`, with a comment flagging it as a "currently invalid, not guaranteed forever" assumption. Flagged separately, not fixed here: `smeldr.dev/mcp`'s core dependency is 4+ minor versions behind current core. Test-only change, no exported symbol, no version bump/tag (matches A228's precedent). Level 1 amendment. | [phase21-archive.md](decisions/phase21-archive.md) |
| A231 | T194: Thread `EdgeClass`/`Confidence` through `Reachability`'s output — `ReachabilityItem` gains both fields, previously discarded during traversal. Confirmed core-only (no `smeldr.dev/mcp` `Reachability` exposure exists), not "across both repos" per T193's own estimate. Surfaced a real gap while implementing: the BFS's node-dedup had no defined tie-break when a node is reachable via multiple edges at the same hop distance — fixed with a deterministic precedence (`asserted` > `observed` > `inferred`, then higher `Confidence`, nil lowest), applied via a collect-then-finalize restructuring of the per-depth loop rather than first-discovery-wins. 4 new tests. No exported symbols removed. Coverage: 96.1%. Level 2 amendment. v1.58.3 → v1.58.4. | [phase21-archive.md](decisions/phase21-archive.md) |
| A232 | T196: Implement `EdgeClass`'s `"observed"` value per T193's own resolved recommendation (`design/edge-class-observed-spike.md`) — new `MCPObserveRelation` (`relations.go`), mirroring `MCPAssertRelation`/`MCPProposeRelation`. Deliberately no `governance.go` or `RecomputeAsserted`/`BulkRecompute` change (both confirmed no-ops per the spike's own resolved judgment calls). `smeldr.dev/mcp` gains a matching `observe_relation` tool plus `"observed"` in `get_relations`' `edge_class` filter enum. 3 new core tests + 6 new mcp tests. No exported symbols changed. Coverage: 96.1% (core), 96.4% (mcp). Level 2 amendment. v1.58.3 → v1.58.4. | [phase21-archive.md](decisions/phase21-archive.md) |
| A233 | T200: Fix `RoleStore.Grant`/`Revoke`'s non-atomic audit-write gap (T199's finding). Checked NEXT.md's own suggested fix before implementing — a naive `txBeginner` mirror on `RoleStore.db` alone would not have worked, since `GovernanceAuditStore` is a pluggable interface with its own separately-bound `db`. Real fix: new unexported `txGovernanceAuditAppender` optional interface + shared `appendAuditRecord` helper; `Grant`/`Revoke` share one transaction with the audit append when the audit store supports it and the DB allows transactions (the common case), falling back to the prior sequential behaviour otherwise — honestly, not silently. Incidentally fixes a second bug: `Grant`'s grantID-resolve `SELECT` failing after a successful `INSERT` no longer orphans the row. `DefineRole` has the same shape but is out of scope, flagged as a follow-up. 4 new tests using a real SQL failure (no mocks) to exercise the rollback. No exported symbols changed. Coverage: 96.1%. Level 2 amendment. | [phase21-archive.md](decisions/phase21-archive.md) |
| A234 | T206: Closes D34: strict transition enforcement + Decision-specific authorization. New `Transition.Strict bool` field + migration. `validateTransition` fail-closed on strict (nil RoleStore or empty actorID return ErrForbidden); unconditionally, row-lookup errors return ErrInternal not nil. `orchDecisionFlow` transitions have `RequiredRole: "admin"` + `Strict: true`. New `authorizeDecisionScope` checking item's Scope against `decisionScopeRoles` map (empty, awaiting policy); wired after `validateTransition` in `updateHandler`, reading pre-decoded item to prevent scope-zeroing attacks. Test caught/prevented that bug. Gap: no MCP tool can change Decision status — separate issue. 19 new + 2 updated tests, 96.1% coverage, patch v1.58.5→v1.58.6. Level 2 amendment. | [phase21-archive.md](decisions/phase21-archive.md) |
| A227 | T183: `smeldr.dev/oauth` — RFC 8707 resource indicators (`Config.Resource`, required; `resource` param required at `/oauth/authorize` and `/oauth/token`; `AuthCode.Resource`/`AccessToken.Resource`) + RFC 9207 `iss` (always in the authorize redirect; `authorization_response_iss_parameter_supported: true` in metadata). `sqlite.go`'s `SQLiteStore` needed its own schema/query changes beyond the `Store` interface (found mid-implementation); new idempotent `migrateAddResourceColumn` (`migrate.go`), same pattern as the v0.3.0 table-rename migration. Breaking Config change; `smeldr/core`'s `example/server/main.go` follow-through ships in this same task (no core-package files touched, no root version bump). Coverage 73.4% → 75.4% (module's 96% gate remains unmet, pre-existing, out of scope). v0.3.0 → v0.4.0. Level 2 amendment. | [phase21-archive.md](decisions/phase21-archive.md) |
| A226 | T179: Fix `updateHandler`'s double-`Save` on publish-via-PUT — deterministically broke `example/blog`'s `TestBlogSignal`/`TestBlogFullServer/audit/recordedOnPublish` with a 409 `rev_conflict`, every run, not intermittently. On a non-Published→Published transition, `updateHandler` saved once, then set `PublishedAt` and saved again on the same in-memory `item`; `SQLRepo.Save`'s rev-based CAS (`WHERE rev = $N`, Amendment A158) increments the stored `rev` without writing it back into the caller's struct, so the second save's `WHERE rev = $N` always missed the row the first save had just advanced, returning `ErrRevConflict` before `AfterPublish` was ever dispatched. Traced via `git log -S`: Amendment A48 (2026-03-15) introduced the double-save correctly, before any CAS existed; A158 (2026-06-20) silently broke it three months later, never re-verified against A48's own path. Checked `processScheduled`/`MCPPublish` (both set `PublishedAt` before their one save, unaffected) and `dynamic.go`'s `setStatus` (single hand-written `UPDATE`, no rev-CAS, unaffected) — `updateHandler` was the only broken site. Fix: merged into one `Save` call, matching `processScheduled`/`MCPPublish`'s own shape. Removed `TestModule_updateHandler_secondSaveError` + its sole fixture `secondSavefailRepo` (the scenario they tested becomes structurally impossible); `TestModule_updateHandler_saveError` still covers the merged save's error path. New `TestModule_updateHandler_publishSetsPublishedAtOneSave` proves the fix against a real `SQLRepo` (not `MemoryRepo`, which can't exercise the rev-CAS): 200 status, `PublishedAt` set, `AfterPublish` fires. No exported symbols changed. Coverage: 96.1%. Level 2 amendment. v1.58.1 → v1.58.2. | [phase20-archive.md](decisions/phase20-archive.md) |
| A202 | T122: T104 Phase B — ValidateFields / ValidatePartialFields / DynamicTypeRepo.ScheduleContent / WithGovernance auto-wire (v1.54.0). ValidateFields(schema, fields map[string]any) and ValidatePartialFields(schema, patch map[string]any) validate create/update paths. DynamicTypeRepo.ScheduleContent(ctx, id, scheduledAt) with state-flow enforcement. CreateDraft/UpdateFields call validators. App.DynamicContentRepo now calls WithGovernance(a.governance) when governance is wired. loadDynamicTypes wires llmsStore compact fragment; rebuildDynamicAIIndex added. Coverage: 96.1%. core v1.54.0. | [phase15-archive.md](decisions/phase15-archive.md) |
| A200 | storage.go time.Time scan fix for SQLite TIMESTAMPTZ columns. timeScanner (sql.Scanner) handles string/[]byte/int64/nil/time.Time sources; scanDest wraps *time.Time scan destinations. Applied in Query[T] and Seq loops. Fixes round-trip queries on draft orchestration items (published_at = zero time). Pre-existing bug exposed by T114 Step 1. No API change. core v1.53.0. | [phase14-archive.md](decisions/phase14-archive.md) |
| A199 | T114 Step 2: get_goal_context MCP tool in smeldr.dev/mcp (v1.27.0). orchestrationToolDefs, isOrchestrationTool, handleOrchestrationTool — dispatches via smeldr.QueryGoalContext; gated on Config.DB != nil; Author role required; returns {goal, linked_decisions, linked_tasks, linked_goals}. tool.go wired in handleToolsList + handleToolsCall. go.mod core v1.51.0 → v1.53.0 (go.sum to be regenerated after core tag). 8 tests. mcp v1.27.0. | [phase14-archive.md](decisions/phase14-archive.md) |
| A198 | T114 Step 1: Goal orchestration type + context-packet query. Goal struct (GoalID, Priority, Band, Size, Description fields). GoalContext struct. QueryGoalContext(ctx, DB, *RelationStore, goalID) (*GoalContext, error) — bidirectional edge query, deduplication, fail-open on nil rs, warn+continue for missing items. orchGoalFlow() — 4 states, 5 transitions, parked→open allowed. CreateOrchestrationTables extended (5th table: smeldr_goals). RegisterOrchestrationTypes extended (5th type + flow). 11 tests. core v1.53.0 (minor bump — new exports). | [phase14-archive.md](decisions/phase14-archive.md) |
| A197 | T121: Config-driven feature-toggle layer (`example/server`). Standalone Go module with own `go.mod` and replace directives. 11 `ENABLE_*` env vars + `OAUTH_ISSUER` gate governance, relations, dynamic content, blocks, media, social, webhooks, redirects, page meta, agents, OAuth. Binary compiles/runs with only `SECRET` set. `migrateDB` always creates `smeldr_tokens`/`smeldr_webhook_endpoints` tables unconditionally. Wiring order load-bearing. `go.work` gains `use ./example/server`. AGENTS.md documents env-var reference. No exported Go symbols, no core version bump. Level 2 amendment. | [phase14-archive.md](decisions/phase14-archive.md) |
| A196 | T113: HTTP/MCP AuthTarget asymmetry fix in smeldr.dev/mcp (v1.26.1). authoriseTool gains `target smeldr.AuthTarget` param. Module-scoped tools (create/update/publish/schedule/archive/delete/list/get) pass `AuthTarget{TypeName: typeName}` — matching the HTTP gate. Infrastructure tools pass `AuthTarget{}`. 23 call sites updated. New parity test (2 sub-cases). Pre-existing coverage 95.3% (StateTool failures unrelated). mcp v1.26.1. | [phase14-archive.md](decisions/phase14-archive.md) |
| A195 | Postgres portability: DATETIME → TIMESTAMP in DDL + pgx CI replace step (v1.52.2). governance.go + migrate.go: replace DATETIME with TIMESTAMP in 8 columns across 6 tables (smeldr_roles created_at/updated_at, smeldr_role_grants created_at, smeldr_tool_policies created_at, smeldr_governance_audit created_at, smeldr_state_flows created_at, smeldr_eval_queue eval_at/created_at). pgx/go.mod: bump smeldr.dev/core v1.38.0 → v1.52.1. pgx integration test: Initial → IsInitial, Terminal → IsTerminal. ci.yml: go mod edit -replace step permanently closes version-lag chicken-and-egg for pgx integration job. First concrete fix under T117 (DDL portability). v1.52.2, no API change. | [phase14-archive.md](decisions/phase14-archive.md) |
| A194 | T112 — Postgres portability for state/governance/dynamic SQL (v1.52.1). Convert `?` → `$N` placeholders, `INSERT OR IGNORE` → `ON CONFLICT (column) DO NOTHING` across state.go, governance.go, migrate.go, dynamic.go. Collapse DefineRole two-step INSERT OR IGNORE + UPDATE to single UPSERT (`ON CONFLICT (name) DO UPDATE SET`; id/created_at INSERT-only). Replace `IS ?` with `IS NOT DISTINCT FROM $N` in Grant. Add integration_core_test.go (//go:build integration, package smeldr) that boots smeldr.App against Postgres 16 via database/sql + pgx/v5/stdlib; extend CI integration job. github.com/jackc/pgx/v5 v5.9.2 added as direct dep. Three SQLite-only constructs deferred: PRAGMA table_info, sqlite_master, DDL types. governance_test.go matchers updated; TestDefineRole_UpdateError removed. Coverage 96.0%. core v1.52.1. | [phase14-archive.md](decisions/phase14-archive.md) |
| A193 | T49 Step 4 (core half): RoleGranted + dual-zone validateTransition + governance mutations (v1.52.0). RoleStore.RoleGranted(ctx, tokenID, roleName, target) — Path B named-role lookup with three scope modes (global/static/dynamic); fail-closed on DB error; pre-collects rows (SQLite constraint). validateTransition: added rs *RoleStore + actorID params; dual-zone pattern — structural checks fail-open (unchanged), required_role checks fail-closed (rs wired + actorID != "" → RoleGranted; error/!ok → ErrForbidden). Module[T].MCPPublish/Schedule/Archive pass m.roleStore + ctx.User().ID. DynamicTypeRepo.rs field + WithGovernance(rs) shallow-copy; SetStatus extracts actorID via local smeldrCtxAccessor. 20 tests (RoleGranted 12 paths, validateTransition 5 paths, DynamicTypeRepo 3 paths). Coverage 96.0%. core v1.52.0. | [phase13-archive.md](decisions/phase13-archive.md) |
| A192 | T49 Step 4 (MCP half): unified authoriseTool in smeldr.dev/mcp (v1.26.0). authoriseTool(ctx, toolName, legacyRole, rs) — three-branch fail-closed: rs==nil → legacy HasRole; rs wired + no token ID → deny; rs wired + token ID → ToolPolicy+Authorized (both fail-closed on error; not-found → deny). Replaces authorise/authoriseEditor/authoriseAdmin (3 dead methods removed). 22 call sites in handleToolsCall updated; rs computed once per request. 8 new tests. mcp v1.26.0. | [phase13-archive.md](decisions/phase13-archive.md) |
| A191 | T49 Step 3: Wire Authorized into module role gate + ToolPolicy (v1.51.0). RoleStore.ToolPolicy(ctx, toolName) exact-match in smeldr_tool_policies; found=false, nil when no row; found=true, requiredOp, nil on hit; fail-closed on DB error. Module[T].roleStore field + setRoleStore(). canReadDrafts(ctx) / checkWriteOp(ctx, op, legacyRole) with 3-branch fail-closed pattern: roleStore==nil → legacy HasRole; ID=="" → deny; ID != "" → call Authorized (error→false). isVisible() converted from func to (Module[T]).isVisible(ctx, item) method; Published always visible, Draft delegates to canReadDrafts. App.governanceModules registration + injection at Handler() time. 20 new tests. core v1.51.0. | [phase13-archive.md](decisions/phase13-archive.md) |
| A190 | T49 Step 2.5: Governance mutation audit trail (v1.50.0). GovernanceAuditRecord struct (ID/ActorTokenID/Action/TargetKind/TargetID/Before/After/CreatedAt); GovernanceAuditStore interface (Append); sqlGovernanceAuditStore + NewGovernanceAuditStore(db); CreateGovernanceAuditTable(db) — smeldr_governance_audit + idx_governance_audit_actor, opt-in not in migrateGovernance; RoleStore.WithAudit(actorTokenID, log) shallow-copy pattern; DefineRole/Grant/Revoke: before-state query → mutation → Append; fail-closed on Append error (non-atomic: mutation may have taken effect; callers should verify state on error not assume rollback). 15 new tests. core v1.50.0. | [phase12-archive.md](decisions/phase12-archive.md) |
| A189 | T49 Step 2: RoleStore + App.Governance() (v1.49.0). governance.go extended with RoleDefinition, RoleGrant, AuthTarget structs; RoleStore (NewRoleStore, DefineRole [rejects trust_level=1], Grant [WHERE NOT EXISTS for NULL anchor], Revoke, ListGrants, Authorized [pre-collect rows → no SQLite deadlock; dynamic scope: edge_class='asserted' + active-edge predicate; static scope: TypeName+":"+ID not slug]); App.Governance(store) validates store.db==cfg.DB then runs migrateGovernance; App.RoleStore() accessor. smeldr.go: governance *RoleStore field. 55 tests. core v1.49.0. | [phase12-archive.md](decisions/phase12-archive.md) |
| A188 | T49 Step 1: Governance schema (v1.48.0). New governance.go: ScopeMode type (ScopeGlobal/ScopeStatic/ScopeDynamic); migrateGovernance creates smeldr_roles, smeldr_role_grants, smeldr_tool_policies + two indexes; seedDefaultRoles (author/editor/admin with full-word ops JSON); seedToolPolicies (MCP tool → required_op mapping, zero behaviour change); migrateTokenGrants (WHERE NOT EXISTS guard — SQLite allows multiple NULLs in UNIQUE, INSERT OR IGNORE unsafe for global-scope grants); fail-open on missing smeldr_tokens. Not wired into New() — opt-in via App.Governance() (Step 2). 22 tests. core v1.48.0. | [phase12-archive.md](decisions/phase12-archive.md) |
| A187 | T23 Step 13: schedule-eval trigger type + DrainEvalQueue (v1.47.0). New exported type TransitionTrigger (FromState, ToState, TriggerClass, TriggerType, Config) persisted by App.RegisterFlow to smeldr_transition_triggers. New table smeldr_eval_queue (id, type_name, item_id, to_state, eval_at, created_at; UNIQUE on type_name+item_id+to_state) with idempotent migration. fireAsyncTriggers extended with itemID; schedule-eval case reads eval_field, INSERTs into smeldr_eval_queue (fail-open). Helper functions resolveItemTable, isNoSuchTable. App.DrainEvalQueue(ctx) (triggered, skipped int, err error) for direct table update + delete; fail-open on nil DB and missing table. orchDecisionFlow wired with two TransitionTriggers (proposed→ratified AND pending-re-evaluation→ratified). smeldr.dev/agent: NewEvalQueueScheduler with default schedule "*/5 * * * *". core v1.47.0. | [phase12-archive.md](decisions/phase12-archive.md) |
| A186 | T23 Step 12: ConflictPolicy enforcement on StateFlow (v1.46.0). Two optional StateFlow fields (ActiveState string, ConflictPolicy) declare the state where uniqueness applies and how to handle violations. ConflictReject returns ErrConflict; ConflictSupersede transitions conflicting items to "superseded" + optional "supersedes" relation. Both fail-open. Wired into MCPPublish/MCPSchedule/MCPArchive and DynamicTypeRepo.SetStatus. core v1.46.0. | [phase12-archive.md](decisions/phase12-archive.md) |
| A185 | T23 Step 11: add create_signal and list_signals MCP tools to smeldr/mcp (v1.25.0). signal_tools.go with direct DB access to smeldr_signals table (Option A); create_signal inserts with status=pending; list_signals fail-open on missing table; both Author role, gated on DB != nil; core dep v1.45.0 → v1.45.1. | [phase12-archive.md](decisions/phase12-archive.md) |
| A184 | Fix data race in state_test.go under go test -race: replace bare bytes.Buffer slog handler target with mutex-protected safeBuf in TestFireAsyncTriggers_asyncTrigger_dispatched, TestFireAsyncTriggers_syncTrigger_skipped, and TestDynamicTypeRepo_SetStatus_fireAsyncTriggers. No production code change. core v1.45.1. | [phase12-archive.md](decisions/phase12-archive.md) |
| A183 | T23 Step 10: Rename `type Signal string` -> `type LifecycleEvent string` in signals.go (all signatures updated); add four orchestration content types (Signal, Task, Decision, Amendment) in orchestration.go; core v1.45.0; dep-bump agent v0.6.2, social v0.9.2, mcp v1.24.2. | [phase11-archive.md](decisions/phase11-archive.md) |
| A182 | T23 Step 9: ScheduledPost delivery flow registration in smeldr/social (v0.9.1). Social.Register in social.go calls app.RegisterFlow with ScheduledPost flow (7 states, 10 transitions: draft→scheduled→queued→delivered/partial/failed, partial/failed→queued retry, terminal→archived). log/slog import added. Fail-open. go.mod: core v1.26.0 → v1.44.3. social v0.9.1. | [phase11-archive.md](decisions/phase11-archive.md) |
| A181 | T23 Step 8: AgentJob state flow registration in smeldr/agent (v0.6.1). Module.Register in flow/module.go calls app.RegisterFlow with AgentJob flow (draft→published, published↔paused, published→archived, paused→archived). paused has SuppressesSignals=true. Fail-open: nil-DB → slog.Error + continue. go.mod: core v1.26.0 → v1.44.3. agent v0.6.1. | [phase11-archive.md](decisions/phase11-archive.md) |
| A180 | T23 Step 7: fireAsyncTriggers(ctx, db, typeName, fromState, toState) in state.go. One JOIN query on smeldr_transition_triggers/transitions/flows for trigger_class='async'. Goroutine per trigger with panic recovery. Unknown trigger_types log slog.Warn. DynamicTypeRepo.SetStatus calls it after successful UPDATE (dynamic.go). Fail-open on all error paths. 10 new tests in state_test.go (including scan-error and rows.Err driver mocks). core v1.44.3. | [phase11-archive.md](decisions/phase11-archive.md) |
| A179 | T23 Step 6: suppressesSignals(ctx, db, typeName, statusName) bool in state.go — pattern matches validateTransition: nil DB → false; custom flow fallback → default; scan error → false (fail-open). notifyAfter early-return guard suppresses all After* signals + afterHook when state.suppresses_signals=true. 9 new tests in state_test.go. core v1.44.2. | [phase11-archive.md](decisions/phase11-archive.md) |
| A178 | T23 Step 5: define_state_flow MCP tool (Admin) in state_tools.go. Calls App.RegisterFlow; type_name required. Three-tier role switch in handleToolsCall (Admin/Editor/Author). parseStates/parseTransitions/boolField helpers. mcp v1.24.1. | [phase11-archive.md](decisions/phase11-archive.md) |
| A177 | T23 Step 4: three state flow MCP tools in smeldr/mcp (transition_item, get_valid_transitions, list_items_by_state). ErrConflict → -32001. core v1.44.1, mcp v1.24.0. | [phase11-archive.md](decisions/phase11-archive.md) |
| A176 | T23 Step 3: validateTransition (unexported) in state.go — checks smeldr_transitions for permitted flow edges; ErrConflict (409) on disallowed; identity + nil DB + non-SQLite → nil. DynamicTypeRepo.SetStatus: calls validateTransition after GetByID; newSetStatusHandler: enum switch removed (empty→400, disallowed→409). Module[T] db DB + setDB; MCPPublish/Archive/Schedule call validateTransition before status change. App.Content: setDB wire. 12 new tests + 1 updated. Coverage 96.0%. v1.44.1. | [phase11-archive.md](decisions/phase11-archive.md) |
| A175 | T23 Step 2: App.RegisterFlow(StateFlow) error + exported types StateFlow, State, Transition in state.go. INSERT OR IGNORE + SELECT id idempotency. validateFlowItems (unexported) SQLite-only unknown-state validation. 12 tests in state_test.go. v1.44.0. | [phase11-archive.md](decisions/phase11-archive.md) |
| A174 | T23 Step 1: add smeldr_state_flows, smeldr_states, smeldr_transitions, smeldr_transition_triggers tables via migrateStateFlows() called from New(). Seed default flow (draft/scheduled/published/archived) idempotently. No exported Go symbols. Prerequisite for T23 Steps 2–9. | [phase11-archive.md](decisions/phase11-archive.md) |
| A173 | Fix FilterTags type assertion in serveblocks.go from .(string) to .([]any) with len > 0 guard. .(string) always silently failed on the array-typed field, making the "not yet supported" warning dead code. Correctness fix only — FilterTags SQL filtering remains out of scope. (T108) | [phase11-archive.md](decisions/phase11-archive.md) |
| A171 | Wire 6 relation MCP tools in smeldr/mcp (assert_relation, propose_relation, get_relations, preview_impact, upsert_relation_kind, list_relation_kinds). Gated by App.RelationStore() != nil; no new ServerOption. Roles: Author/Editor/Admin per tool. Output uses snake_case map helpers. mcp v1.23.0. | [phase11-archive.md](decisions/phase11-archive.md) |
| A170 | docs(cleanup): archive A151–A157, migrate CLAUDE.md, backfill A167–A169 | [phase11-archive.md](decisions/phase11-archive.md) |
| A169 | T106: slug collision check + dynamic route registration | [phase10-archive.md](decisions/phase10-archive.md) |
| A168 | T106: Listable, Serves[T], Aggregate, App.Route | [phase10-archive.md](decisions/phase10-archive.md) |
| A167 | T106: smeldr_routes table + redirect migration | [phase10-archive.md](decisions/phase10-archive.md) |
| A165 | T06 step 7: Layer 3a structural sweep | [phase10-archive.md](decisions/phase10-archive.md) |
| A163 | T06 step 6: MCP relation kind tools | [phase10-archive.md](decisions/phase10-archive.md) |
| A162 | T06 step 5: MCP relation tools | [phase10-archive.md](decisions/phase10-archive.md) |
| A161 | T06 step 4: Layer 2 reactive cascade signal | [phase10-archive.md](decisions/phase10-archive.md) |
| A160 | T06 step 3: Layer 1 save-path relation recompute | [phase10-archive.md](decisions/phase10-archive.md) |
| A159 | T06 step 2: relation schema + stores | [phase10-archive.md](decisions/phase10-archive.md) |
| A158 | Node.Rev optimistic-concurrency token | [phase10-archive.md](decisions/phase10-archive.md) |

### Core — [decisions/core.md](decisions/core.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| 1 | Node identity | Locked | 2025-06-01 |
| 2 | Storage model | Locked | 2025-06-01 |
| 3 | Head/SEO ownership | Locked | 2025-06-01 |
| 4 | Rendering model | Locked | 2025-06-01 |
| 5 | Cookie consent enforcement | Locked | 2025-06-01 |
| 6 | Context type | Locked | 2025-06-01 |
| 7 | AIDoc format | Locked | 2025-06-01 |
| 8 | llms.txt generation | Locked | 2025-06-01 |
| 9 | Sitemap strategy | Locked | 2025-06-01 |
| 10 | Validation API | Locked | 2025-06-01 |
| 11 | Internationalisation | Locked | 2025-06-01 |
| 12 | Image type | Locked | 2025-06-01 |
| 13 | RSS feeds | Locked | 2025-06-01 |
| 14 | Content lifecycle | Locked | 2025-06-01 |
| 15 | Role system | Locked | 2025-06-01 |
| 16 | Error handling model | Locked | 2025-06-01 |
| 17 | Redirects and content mobility | Locked | 2025-06-01 |
| 18 | Licensing strategy | Locked | 2025-06-01 |
| 19 | MCP (Model Context Protocol) support | Locked | 2025-06-01 |
| 20 | Configuration model | Locked | 2025-06-01 |
| 21 | forge.Context is an interface | Locked | 2025-06-01 |
| 22 | Storage interface and database drivers | Locked | 2025-06-01 |
| A19 | `storage.go`: `SQLRepo[T]` production repository | Agreed | 2026-03-07 |
| A20 | `forge.go`: `RedirectStore`, `App.Redirect()`, fallback handler | Agreed | 2026-03-07 |
| A21 | `forge.go`: `/.well-known/redirects.json` handler | Agreed | 2026-03-07 |
| A22 | `forge.go`: `App.RedirectManifestAuth()` | Agreed | 2026-03-07 |
| A23 | `node.go`: `db` struct tags on `Node` time fields | Agreed | 2026-03-07 |
| A24 | `context.go`: `NewBackgroundContext` | Agreed | 2026-03-07 |
| A25 | `module.go`: `processScheduled` + helpers | Agreed | 2026-03-07 |
| A26 | `forge.go`: scheduler wiring | Agreed | 2026-03-07 |
| A27 | `middleware.go`: `forge.Authenticate(AuthFunc)` | Agreed | 2026-03-08 |
| A28 | Auto-detect `Headable` in `Module[T]` | Agreed | 2026-03-08 |
| A29 | `errors.go` error handling gaps | Agreed | 2026-03-11 |
| A30 | `module.go` error handling gaps | Agreed | 2026-03-11 |
| A31 | `templates.go` error handling gaps | Agreed | 2026-03-11 |
| A32 | `middleware.go` error handling gaps | Agreed | 2026-03-11 |
| A33 | `module.go` route mounting order bug (sitemap + feed) | Agreed | 2026-03-11 |
| A34 | `module.go` + `forge.go` startup rebuild for derived content | Agreed | 2026-03-11 |
| A35 | `module.go` content negotiation capability gating | Agreed | 2026-03-11 |
| A36 | `module.go` startup capability mismatch detection | Agreed | 2026-03-11 |
| A37 | `WriteError` pipeline — replace `http.Error`/`http.NotFound` bypasses | Agreed | 2026-03-12 |
| A38 | `auth.go`: `SignToken` error return implements `forge.Error` | Agreed | 2026-03-12 |
| A39 | `Module[T]`: cache sweep goroutine lifecycle and `Stop()` method | Agreed | 2026-03-12 |
| A40 | Rename `FeedDisabled()` → `DisableFeed()` and `forgeLLMSEntries` → `forgeLLMsEntries` | Agreed | 2026-03-12 |
| A41 | `Module[T]`: debounce callback must use `NewBackgroundContext`, not stashed request context | Agreed | 2026-03-12 |
| A42 | `forge.go`: `Config.Version` field and `App.Health()` endpoint | Agreed | 2026-03-12 |
| A43 | `NewSQLRepo` pointer type documentation (amends Decision 22) | Agreed | 2026-03-14 |
| A44 | `dbFields`: flatten embedded (anonymous) struct fields via `[]int` index path | Agreed | 2026-03-15 |
| A45 | `Config.Auth` field + default `BearerHMAC` wired in `New()` | Agreed | 2026-03-15 |
| A46 | `markdown.go`: minimal Markdown→HTML renderer added to `TemplateFuncMap` | Agreed | 2026-03-15 |
| A47 | `templatehelpers.go`: `forge_markdown` delegates to `renderMarkdown` | Agreed | 2026-03-15 |
| A48 | `module.go`: set `PublishedAt` on manual publish in `updateHandler` | Agreed | 2026-03-15 |
| A49 | `mcp.go`/`module.go`/`forge.go`: `MCPModule` contract — `mcpOption` carries ops; export `MCPMeta`, `MCPField`, `MCPModule`; `Module[T]` implements 10 MCP methods; `App.MCPModules()` | Agreed | 2026-03-16 |
| A50 | `auth.go`/`forge.go`/`context.go`/`forge-mcp/mcp.go`: `VerifyBearerToken`, `App.Secret()`, `NewContextWithUser`, `Server` secret auto-inherit | Agreed | 2026-03-16 |
| A51 | `templates.go`: `twitter:card` derives from `Head.Type` — `Article`/`Product` emit `summary_large_image` without requiring an explicit image | Agreed | 2026-03-17 |
| A52 | `module.go`/`forge-mcp/mcp.go`: `[]string` fields typed as `"array"` in `MCPSchema`/`inputSchema`; comma-string coercion in `MCPCreate`/`MCPUpdate` | Agreed | 2026-03-17 |
| A53 | `module.go`: `negotiate()` prefers `text/html` over `application/json` when `Accept` is absent or `*/*` and templates are configured | Agreed | 2026-03-18 |
| A56 | `head.go`: `AbsURL(base, path string) string` helper for building absolute URLs in `Head()` implementations | Agreed | 2026-03-20 |
| A57 | `storage.go`: `quoteIdent()` helper — double-quote all generated SQL identifiers to handle reserved keywords | Agreed | 2026-03-20 |
| A58 | `forge.go`: `forgeVersions()` — read `runtime/debug.ReadBuildInfo()` for `/_health` and startup log; remove `"version"` key from `Health()` response | Agreed | 2026-03-20 |
| A59 | `forge.go`: `httpsRedirect()` — exempt `/_health` from HTTPS redirect so reverse-proxy health checks receive 200 on plain HTTP | Agreed | 2026-03-20 |
| A60 | `forge.go`: `New()` calls `MustConfig()` automatically — configuration errors are always caught at startup, never at first request | Agreed | 2026-04-02 |
| A61 | `social.go`/`schema.go`/`templates.go`: `OGDefaults`, `AppSchema` SEOOptions; `forge:head` receiver changed from `Head` to `TemplateData` | Agreed | 2026-04-02 |
| A62 | `forge.go`/`templates.go`/`module.go`: `App.Partials(dir)`, `App.MustParseTemplate(path)`, `loadPartials`, `setPartials`, `parseOneTemplate` accepts partials — shared partial templates injected into all module and custom handler templates | Agreed | 2026-04-02 |
| A63 | `head.go`/`templates.go`/`templatedata.go`/`forge.go`/`module.go`: `HeadAssets`, `FaviconLink`, `ScriptTag` SEOOption — injects static assets (preconnect, stylesheets, favicons, scripts) into forge:head on every page via `app.SEO(&HeadAssets{...})` | Agreed | 2026-04-03 |
| A64 | `head.go`/`templatedata.go`: `PageHead` exported struct — embeddable head fields for custom handler data structs; `TemplateData[T]` refactored to embed `PageHead` anonymously | Agreed | 2026-04-03 |
| A65 | `module.go`/`templatedata.go`/`templates.go`: `ContextFunc` module option — per-request extra data injected into `TemplateData.Extra` for list and show renders | Agreed | 2026-04-04 |
| A88 | `forge.go`: `App.Webhooks(store *WebhookStore)`, `App.WebhookPool() WebhookJobQueue`, `App.injectWebhookHooks()` — wires outbound webhook infrastructure into the App; pool started/stopped with server lifecycle. | Agreed | 2026-05-08 |
| A89 | `module.go`: `afterHook`/`setAfterHook`/`notifyAfter` — post-lifecycle callback slot on `Module[T]`; `notifyAfter` wraps `dispatchAfter`+`afterHook`; `MCPSchedule` dispatches `AfterSchedule`. CLI parity: `forge webhook` ships with `forge-mcp` webhook tools (A86 gap closed). | Agreed | 2026-05-08 |
| A90 | `REFERENCE.md`: replace hardcoded `1.16.0`/`1.6.1` version literals in health endpoint examples with `x.y.z` placeholder (3 occurrences). `FEATURELIST.md`: correct `delete_[type]` role from `Author+` to `Editor+` — matches `authoriseEditor()` enforcement in `forge-mcp/tool.go`. | Agreed | 2026-05-07 |
| A91 | `webhook.go`: `WHERE active = 1` → `WHERE active` (Postgres BOOLEAN parity). DDL godoc: `DEFAULT 1`→`DEFAULT TRUE`, `DATETIME`→`TIMESTAMPTZ` in `webhook.go`; `BLOB`→`BYTEA`, `DATETIME`→`TIMESTAMPTZ` (5 occurrences) in `outbound.go`. `README.md`: add token management reference link. | Agreed | 2026-05-08 |
| A92 | `auth.go`: `encodePreviewToken(prefix,slug,...)`/`decodePreviewToken` (internal, prefix-bound). `forge.go`: `Config.PreviewTokenExpiry`, `App.GeneratePreviewToken(prefix,slug)`, `App.BaseURL()`. `module.go`: `secret` field, `setSecret`, preview bypass in `showHandler`. forge-mcp: `create_preview_url` Admin tool. forge-cli: `preview` subcommand. Milestone 12 — v1.18.0. | Agreed | 2026-05-08 |
| A93 | `auth.go`: `encodeUploadToken(secret,ttl)`/`decodeUploadToken` (internal). `forge.go`: `Config.MediaUploadTokenExpiry`, `App.GenerateUploadToken()`, `App.ValidateUploadToken(token)`. forge-media: `UploadToken` header in `handleUpload`, image-only MIME whitelist for token uploads, AVIF support, hex filename prefix. forge-mcp: `create_upload_token` Author+ tool. forge-cli: media subcommands documented + AVIF. Milestone 13 — v1.19.0. | Agreed | 2026-05-09 |
| A94 | Signal bus: `SignalEvent`, `afterHookMeta`, `buildSignalEvent` (`signals.go`). `App.OnSignal`, `App.dispatchBus`, `App.wireSignalBus` replacing `injectWebhookHooks` (`forge.go`). `webhookDispatch` (`webhook.go`). `OutboundDelivery` interface (`outbound.go`). `notifyAfter` signature extended with `afterHookMeta`. Milestone 14 — v1.20.0. | Agreed | 2026-05-11 |
| A95 | `mergeFileConfig`: field-level `OGDefaults` merge — `og_image` in `forge.config` overrides Go-code `Image.URL`; all other `OGDefaults` fields retain Go-code values. Only `forge.config` key designed to take precedence over Go code. No exported symbols changed. v1.21.0. | Agreed | 2026-05-14 |

### Token management — [decisions/auth.md](decisions/auth.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| [decisions/auth.md](decisions/auth.md) | Token management archive | Archive | 2026-05-17 |
| D25 | Token management | Locked | 2026-04-05 |
| A66 | `auth.go`/`forge.go`/`forge-mcp`: `TokenStore` — named revocable bearer tokens, DB-backed `VerifyBearerToken`, three Admin MCP tools (`create_token`, `list_tokens`, `revoke_token`) | Agreed | 2026-04-05 |
| D26 | `auth.go`/`errors.go`/`forge-mcp/tool.go`: last-admin guard on `TokenStore.Revoke` — `ErrLastAdmin` sentinel (409); `Revoke` refuses to revoke the last active admin token; `revoke_token` MCP tool surfaces actionable message | Agreed | 2026-04-06 |
| A83 | `auth.go` / `forge.go`: `TokenStore.ensureBootstrap` — auto-creates a bootstrap admin token (slog.Warn) when `forge_tokens` is empty at startup. `forge-cli/init.go`: new `init` subcommand bootstraps a new instance using the bootstrap token. `forge-cli` v0.3.0. | Agreed | 2026-05-04 |

### Content API — [decisions/content-api.md](decisions/content-api.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| [decisions/content-api.md](decisions/content-api.md) | Content API archive | Archive | 2026-05-17 |
| D27 | `mcp.go`/`module.go`/`forge-mcp/mcp.go`: field format semantics — `forge_format` and `forge_description` struct tags populate `MCPField.Format` and `MCPField.Description`; forge-mcp emits `"description"` key in JSON Schema properties with priority logic | Agreed | 2026-04-07 |
| A67 | `templatehelpers.go`: `forgeHTML` / `forge_html` — trusted raw HTML passthrough added to `TemplateFuncMap` | Agreed | 2026-04-05 |
| A74 | `head.go`/`templates.go`/`example_test.go`: rename `FaviconLink` → `HeadLink`; rename `HeadAssets.Favicons []FaviconLink` → `HeadAssets.Links []HeadLink` — any `<link>` element, not icons only | Agreed | 2026-04-18 |
| A75 | `markdown.go`: `renderMarkdown` HTML passthrough — lines whose trimmed form starts with `<` are emitted verbatim without HTML-escaping, unblocking HTML blocks in trusted body content | Agreed | 2026-04-22 |
| A77 | `head.go`/`module.go`/`templates.go`: `ListHeadFunc` option — new `listHeadFuncOption[T]` type; `listHeadFunc any` field on `Module[T]`; `renderListHTML` resolves list head via `listHeadFunc`; fixes empty `<title>` on module list pages | Agreed | 2026-05-02 |

### Documentation — [decisions/docs.md](decisions/docs.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| [decisions/docs.md](decisions/docs.md) | Documentation archive | Archive | 2026-05-17 |
| D28 | `forge-cli/`: operator CLI — stdlib-only submodule; content CRUD + lifecycle via HTTP REST; token management via MCP JSON-RPC; YAML-subset frontmatter parser; `forge-cli/v0.1.0` | Agreed | 2026-04-07 |
| A69 | `README.md`: shortened to <150 lines; `REFERENCE.md`: new full API reference file; `example/blog/main.go` package comment updated to v1.11.0 | Agreed | 2026-04-14 |
| A70 | `README.md`: tagline, named value section (15 features), remove duplicate table row, `(*Post)(nil)` comment, real AfterPublish body, examples pointer, remove flat bullet list | Agreed | 2026-04-14 |
| A71 | `README.md`: replace tagline with plain-language framework description; add 30-second start section (clone + run) before "What Forge gives you" | Agreed | 2026-04-15 |
| A72 | `VISION.md`: insert "What Forge is" (typed state layer for AI agents); insert "The two-layer model" (Core AGPL / Cloud commercial); replace Roadmap (Phases 1–2 ✅ DONE, Phase 3 Cloud private beta, Phase 4 Cloud GA) | Agreed | 2026-04-18 |
| A76 | `go.mod` (all modules): bump minimum Go version `1.22` → `1.26.2`; rename all module paths from `github.com/forge-cms/...` to `forge-cms.dev/...`; update all imports, documentation, and `forgeVersions()` prefix logic | Agreed | 2026-04-30 |
| A84 | `REFERENCE.md`: accuracy fixes and gap-fill for v1.16.0 — corrects 5 inaccuracies (version examples, broken links, RateLimit section, `app.Content` fallback path); adds 6 missing sections (TokenStore, NavTree, OGDefaults/AppSchema, AbsURL, SeqRepository, forge-cli); adds `ErrLastAdmin` sentinel. | Agreed | 2026-05-05 |
| A85 | `.github/copilot-instructions.md`: new "Docs and content workflow" section inserted between "Standard step workflow" and "Release tagging". `FEATURELIST.md`: new file — complete feature list for v1.16.0. | Agreed | 2026-05-05 |
| A86 | `.github/copilot-instructions.md`: new "CLI and MCP tool parity" section — every MCP tool must have a CLI equivalent in the same release; notes current nav commands gap. | Agreed | 2026-05-05 |

### Media — [decisions/media.md](decisions/media.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| [decisions/media.md](decisions/media.md) | Media archive | Archive | 2026-05-17 |
| A73 | `forge.go`/`config.go`: add `MediaPath` and `MediaMaxSize` fields to `Config`; parse `media_path` and `media_max_size` from `forge.config` file; add `App.Config() Config` read-only accessor for forge-media submodule access | Agreed | 2026-04-25 |
| D31 | `forge-media/`: new optional submodule — `MediaStore` interface, `LocalMediaStore`, HTTP handlers (`Register`), `forge.MCPModule` implementation, `MediaRecord.GetSlug()`; `forge-mcp`: `WithModule` server option | Agreed | 2026-04-18 |
| A79 | `forge-media/media.go`: `LocalMediaStore.Store()` and `.Delete()` use `os.Root` (Go 1.24+) instead of `filepath.Join` — path traversal prevented at OS level. Security fix. Two new tests added. | Agreed | 2026-05-04 |

### Navigation — [decisions/nav.md](decisions/nav.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| [decisions/nav.md](decisions/nav.md) | Navigation archive | Archive | 2026-05-17 |
| D29 | `nav.go`/`forge.go`/`templatedata.go`/`templates.go`/`module.go`/`forge-mcp`: NavTree — first-class navigation abstraction; `NavItem`, `NavTree`, `NavModeDB`/`NavModeCode`, `App.Nav()`, `App.NavTree()`, `TemplateData.Nav`, four MCP nav tools (Editor role) | Agreed | 2026-04-11 |
| D30 | `config.go`/`forge.go`: forge.config file-based configuration — `loadConfigFile`, `mergeFileConfig`; `Config.AppSchema`, `Config.OGDefaults`; `MustConfig` loads `forge.config` (or `FORGE_CONFIG` env var path); Go-code fields always win; `secret` key panics | Agreed | 2026-04-11 |
| A82 | `forge.go` / `config.go` / `static.go`: `Config.Dev bool` + `App.Static(prefix, prod, devDir)` + forge.config `dev` key. Dev mode serves from disk; prod mode serves embedded FS with immutable Cache-Control. Replaces per-site boilerplate. | Agreed | 2026-05-04 |

### Storage — [decisions/storage.md](decisions/storage.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| [decisions/storage.md](decisions/storage.md) | Storage archive | Archive | 2026-05-17 |
| A68 | `storage.go`/`module.go`: doc comments on `Table` and `At` extended to surface irregular pluralisation pitfalls (Story → "storys") | Agreed | 2026-04-09 |
| A78 | `node.go`: `ValidateStruct` unexported to `validateStruct`; `RunValidation` is now the sole public entry point for struct-tag validation. Breaking change: removes exported symbol. | Agreed | 2026-05-04 |
| A80 | `storage.go`: `SeqRepository[T]` optional interface + `Seq` methods on `MemoryRepo[T]` and `SQLRepo[T]` — lazy `iter.Seq2[T, error]` streaming without full result-set load. Additive; `Repository[T]` unchanged. | Agreed | 2026-05-04 |
| A81 | `go.mod`: `modernc.org/sqlite` added as test-only dependency; enables `TestRepoParity_SQLRepo` against real in-memory SQLite. Exception to zero-dep rule: CGO-free, test-only, single file, documented precedent. | Agreed | 2026-05-04 |

### Phase 3 Archive — [decisions/phase3-archive.md](decisions/phase3-archive.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| A102 | `module.go`: `APIOnly()` module option — marks a module as REST/MCP/CLI-only with no public HTML surface. `GET /{prefix}` and `GET /{prefix}/{slug}` with `Accept: text/html` return 404. JSON routes and all MCP tools unchanged. `APIOnly()` + `SingleInstance()` panics at startup. v1.24.0. | Agreed | 2026-05-22 |
| A103 | `auth.go`: `VerifyTokenString(token string, secret []byte, store *TokenStore) (User, bool)` — verifies a raw bearer token without `*http.Request`. Identical to `VerifyBearerToken` but takes the token string directly; DB lookup uses `context.Background()`. Enables forge-oauth (and other downstream libraries) to validate Forge tokens without importing the HTTP layer. v1.25.0. | Agreed | 2026-05-24 |
| A104 | `forge.go`: `/_health` JSON key and startup log rename. `"forge"` → `"core"`, `"forge_mcp"` → `"mcp"` in `/_health` response; startup log prefix `"forge: "` → `"smeldr: "`. Follows module path rename (T59 Phase 0C). Breaking change for health monitors. | Agreed | 2026-05-26 |
| A105 | T59 Phase 2.4: all smeldr.dev/* modules tagged and published. First Go-resolvable versions on smeldr.dev/* paths. 7/8 confirmed via go get; smeldr.dev/pgx blocked pending vanity config fix (architect decision required). | Agreed | 2026-05-27 |
| A106 | T59 doc rename: forge-cms.dev → smeldr.dev across all core documentation. Renamed all forge-cms.dev/* → smeldr.dev/*, github.com/forge-cms/* → github.com/smeldr/*, and prose module names (forge-mcp → smeldr.dev/mcp, etc.) across 10 doc files. Docs only — no code changes. | Agreed | 2026-05-28 |
| A107 | T62 package rename: `package forge` → `package smeldr` in all 75 root-package Go files. 9 template function names renamed (forge:head → smeldr:head, forge_markdown → smeldr_markdown, etc.), 2 struct tag keys (forge_format → smeldr_format, forge_description → smeldr_description), 2 cookie names (forge_csrf → smeldr_csrf, forge_consent → smeldr_consent). All standalone modules (mcp, media, social, agent, oauth, cli) updated. Breaking change — v1.26.0. | Agreed | 2026-05-28 |
| A108 | T64+T65: `forge.config` → `smeldr.config`, `FORGE_CONFIG` → `SMELDR_CONFIG` (breaking for operators). Error prefix `"forge: "` → `"smeldr: "` in ~48 strings across 14 files. `skills/forge.md` → `skills/smeldr.md` (core + common repos). No exported API or schema changes. v1.27.0. | Agreed | 2026-05-28 |
| A109 | T66: `forge_*` → `smeldr_*` DB table rename (7 tables); `migrateLegacyTableNames` auto-migration at `New()` for SQLite. PostgreSQL operators must migrate manually. v1.28.0. | Agreed | 2026-05-28 |
| A110 | T63: `SiteConfig` singleton — global site-configuration content type in core. `SiteConfig` struct with 5 fields (`site_name`, `title_separator`, `og_image`, `x_handle`, `head_script`); `NewSiteConfigModule(db)` factory; `CreateSiteConfigTable(db)` DDL helper. Configurable via MCP after first deploy — no rebuild required. v1.29.0. | Agreed | 2026-05-28 |
| A111 | T74+T67: `HeadAssets.RawHead template.HTML` — verbatim HTML injected into `<head>` after all other HeadAssets output; zero value is no-op (T74). Validation/auto-slug struct tag key renamed `forge:"required"` → `smeldr:"required"` — breaking for operators with custom content types (T67). v1.30.0. | Agreed | 2026-05-29 |
| A112 | T71: `xAuthBase` changed `"https://twitter.com"` → `"https://x.com"` in social/twitter.go — fixes X OAuth login loop caused by session cookie domain mismatch. social v0.7.4. | Agreed | 2026-05-29 |
| A113 | T57 oauth: `POST /oauth/revoke` per RFC 7009 — `revokeHandler` added; always 200 OK; revokes refresh tokens via `DeleteRefreshToken`; access tokens expire naturally. oauth v0.1.4. | Agreed | 2026-05-29 |
| A114 | T57 cli: `smeldr-cli oauth revoke <token>` — POSTs to `FORGE_URL/oauth/revoke`; CLI parity with A113. cli v0.9.3. | Agreed | 2026-05-29 |
| A115 | T58: `forgemcp.Server.Register(app *smeldr.App)` — mounts all MCP+OAuth routes on forge App in one call; delegates to `s.Handler()` mux. `Handler()` unchanged. go.mod: core v1.30.0, oauth v0.1.4. mcp v1.13.0. | Agreed | 2026-05-29 |

### Phase 9 Archive — [decisions/phase9-archive.md](decisions/phase9-archive.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| A151 | T104 Inc 1: schema generalisation + content-type registry | Agreed | 2026-06-15 |
| A152 | T96: ContentList block resolver | Agreed | 2026-06-15 |
| A153 | T104 Inc 2: dynamic content substrate | Agreed | 2026-06-16 |
| A154 | T104 Inc 2 patch: operator-controlled URL routing | Agreed | 2026-06-16 |
| A155 | security: SSRF fix in outbound webhook transport | Agreed | 2026-06-17 |
| A157 | T72 PageMeta — per-path SEO override layer | Agreed | 2026-06-19 |

### Recent — [decisions/recent.md](decisions/recent.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| A135 | T101: standalone-module brand-prose sweep. `forgeCtx`→`smeldrCtx`, `forgeFallback`→`fallback` (unexported renames in mcp transport.go). Godoc/comment/README/help-text "Forge"→"Smeldr" across mcp, media, social, cli. `TEST_FORGE_CLI_*` test env-var identifiers renamed. `verifyForgeSignature` example renamed. Preserved: `WithForgeFallback`, `forge://` URIs, `FORGE_*` env var docs (T86/T87), `ForgeURL`, `X-Forge-Signature`, test fixture content, historical CHANGELOG narratives. No exported-symbol or behaviour change. oauth unchanged (all remaining hits are test fixtures). mcp v1.17.1 · media v1.4.1 · social v0.8.1 · cli v0.14.1. Closes T101. | Agreed | 2026-06-07 |
| A136 | `list_storys` → `list_stories`: new `pluralSnake()` helper + `isVowel()` applies consonant-y → ies rule for MCP list tool name generation. `moduleForAdminList()` reverse lookup handles "ies" suffix. Three new tests. mcp v1.17.2. | Agreed | 2026-06-08 |
| A137 | `processScheduled` save-error resilience: `return err` on `repo.Save` failure replaced with `slog.Warn + continue` — a Save failure for one item no longer halts remaining scheduled items in the same tick. `scheduler.go`: errors from `processScheduled` captured and logged (was silently ignored). `scheduler_test.go`: `TestProcessScheduled_continuesAfterSaveError` + `failOnSaveRepo[T]` helper. core v1.36.1. | Agreed | 2026-06-08 |
| A138 | X post body length validation now uses t.co URL weighting: `xWeightedBodyLen()` counts each URL as `xTcoURLLen` (23) chars instead of its actual rune length. `xURLRegexp` package-level var. `publish()` in twitter.go updated. `TestXWeightedBodyLen` (5 cases) + `TestPublishBodyLen_tcoWeighting` + `xAPIRedirectTransport` test helper. social v0.8.2. | Agreed | 2026-06-08 |
| A139 | `migrateLegacyTableNames` idempotency fix: destination-existence check prevents "table already exists" on partial re-run. `slog.Warn + continue` when destination already exists. `TestMigrateLegacyTableNames_destinationExists` + `TestMigrateLegacyTableNames_sourceOnly`. core v1.36.2. | Agreed | 2026-06-09 |
| A140 | X publisher debug logging: `slog.Debug` before each HTTP call (method, URL — no credentials); `slog.Warn` on non-2xx (status, body excerpt ≤256 chars, `X-Request-Id`). `TestPublish_logsWarnOnNonSuccess` + `slogCapture`. social v0.8.3. | Agreed | 2026-06-09 |
| A141 | X media upload: replace single-POST with mandatory INIT/APPEND/FINALIZE three-step chunked protocol. Missing `command` field caused gateway 403 before reaching X's API handler. `strconv` import added; slog.Debug/Warn applied to each step. Happy-path test updated to assert all three commands; APPEND-403 and FINALIZE-403 error cases added. social v0.8.4. | Agreed | 2026-06-10 |
| A142 | X OAuth 2.0 scope configurable: `Scopes []string` added to `xConfig`; `effectiveScope()` method returns default (with `media.write`) or joined custom scopes; `authURL()` uses `effectiveScope()`; `social.go` threads `cfg.Scopes` through in both `xConfig` construction sites; `scope` field description in `config_mcp.go` updated to cover both X and Mastodon defaults. `TestEffectiveScope` (2 cases). social v0.8.5. | Agreed | 2026-06-10 |
| A143 | Streamable HTTP SSE response mode: `messageHandler` branches on `Accept: text/event-stream` — SSE path sets `Content-Type: text/event-stream` and writes `data: <json>\n\n` + flush; JSON path unchanged. `POST /mcp/message` unaffected (old clients never send that Accept header). `TestMessageHandler_AcceptSSE_ReturnsEventStream` + `TestMessageHandler_NoAccept_ReturnsJSON`. mcp v1.18.0. | Agreed | 2026-06-10 |
| A144 | media `StatsExtProvider`: `stats.go` adds `StatsKey()→"media"` and `ProvideStats()` on `*Server`. Two queries: `COUNT(*)+SUM(size_bytes)` for `file_count`/`total_bytes`; `GROUP BY mime_type` for `by_type`. `TestProvideStats` (seeded counts+totals) + `TestProvideStats_empty`. media v1.5.0. | Agreed | 2026-06-10 |
| A145 | T94: content-type instances as block-section parents. `ContentParentProvider` interface (`BlockParentTypeName`, `HasBlockParent`) + `blockHostProvider` unexported marker in new `block_host.go`. `BlockHost() Option` + `blockHost bool` field on `Module[T]`; `BlockParentTypeName()`/`HasBlockParent()`/`blockHostEnabled()` methods. `App.blockParents []ContentParentProvider`; `RegisterBlockParent`/`BlockParents()` in `stats.go`; auto-wired in `App.Content()` via `blockHostProvider` type assertion. mcp: `Server.blockParents` field; `WithBlocks()` populates from `app.BlockParents()`; `resolveParentType` helper tries DynamicNode repo first, then iterates providers; `addEdge` uses it. Tool description updated. Body and sections coexist (independent data paths). Ride-along T98: mojibake in `integration_full_test.go` G-index legend replaced with plain hyphens. core v1.37.0 · mcp v1.19.0. | Agreed | 2026-06-10 |
| A146 | T97: Schema-defined block types. `SchemaField`, `ContentTypeSchema`, `SchemaStore`, `CreateSchemaTable`, `SeedBlockTypeSchemas`, `ValidateBlockFields` in core `schemas.go`. 16 canonical types seeded. MCP: `get_content_type_schema`, `list_content_type_schemas` discovery tools; `create_<type_name>` × 16 typed tools generated at startup. `create_node`/`update_node` validate fields when schema exists; `ErrNotFound` → pass-through (backwards compat). Supplements, never replaces `create_node`. core v1.38.0 · mcp v1.20.0. | Agreed | 2026-06-10 |
| A147 | T102: DB table rename forge_* → smeldr_* in standalone modules. `migrateLegacyTableNames` added to oauth (3 tables), media (1 table), social (8 tables) — same pattern as core A109. Each module's setup function calls migration before `CREATE TABLE IF NOT EXISTS`. SQLite-only probe via `sqlite_master`; non-SQLite returns nil. Idempotent: source-gone skips, both-exist warns+skips. Single transaction via `BeginTx`. social: existing column-migrations (`actor_id`, `code_verifier`) updated to reference renamed tables. oauth v0.3.0 · media v1.6.0 · social v0.9.0. | Agreed | 2026-06-11 |
| A148 | Security: bump `github.com/jackc/pgx/v5` from v5.8.0 to v5.9.2 in `smeldr.dev/core/pgx`. Resolves two Dependabot alerts (1 Critical, 1 Low). No code changes — dependency version bump only. pgx/v0.1.1. | Agreed | 2026-06-11 |
| A149 | Dependency alignment: bump `smeldr.dev/core` from v1.29.0 to v1.38.0 in `smeldr.dev/core/pgx`. go.mod + go mod tidy; tests green; no code changes. pgx/v0.1.2. | Agreed | 2026-06-12 |
| A150 | T102/N60: statement coverage lifted from 95.9% to 97.1% — 41 new tests in `coverage_test.go` covering 51 uncovered blocks across 12 source files; `codecov.yml` gate at 96%/0.5%. Test-only; no exported symbols changed. | Agreed | 2026-06-14 |
| A134 | T100 Step 7: core + common docs selector sweep. `forge*.` selectors → new package names in `common/agent/skills/smeldr.md`, `core/docs/REFERENCE.md`, `core/docs/FEATURELIST.md`, `core/AGENTS.md`, `core/example/blog/main.go` + `main_test.go`. `forgeoauth.`→`oauth.`, `forgemcp.`→`mcp.`, `forgesocial.`→`social.`, `forgeagent`→`agentflow` alias. `forge-cli`→`smeldr-cli` throughout CLI examples. Version line bump (mcp v1.17.0, oauth v0.2.0, media v1.4.0, cli v0.14.0, social v0.8.0). `socialSrv` collision fix; `mcp.WithForgeFallback()` kept. Install path `cmd/smeldr-cli`. `.forge-cli.env` legacy note + `FORGE_*` fallback mentions + `## forge-*` section headers preserved (T86/T87 + T101). `go build ./...` green. Docs-only, no version bump. Closes T100. | Agreed | 2026-06-07 |
| A133 | T100 Step 5: cli binary rename `forge-cli` → `smeldr-cli` + `logs` command (smeldr.dev/cli v0.14.0). All user-facing `forge-cli` strings renamed in 21 source files; .go files moved to `cmd/smeldr-cli/` (binary naming fix; install path `go install smeldr.dev/cli/cmd/smeldr-cli@latest`). New `smeldr-cli logs` command (T79): `GET /_logs` direct HTTP (not MCP), tabwriter table TIMESTAMP/LEVEL/SEQ/MESSAGE, `--level`/`--limit`/`--since`/`--json`, dropped-entry footer; `logEntry.Seq`+`logsEnvelope.Dropped` both `uint64`. README rewritten; CHANGELOG header + [0.14.0] section. Preserved: `.forge-cli.env` filename + `FORGE_*` env vars (T86/T87); gate = 6 hits (all T86/T87). Closes T79 CLI half; closes N57 forge-cli binary name reversal. | Agreed | 2026-06-07 |
| A132 | T100 Step 4: social package rename `forgesocial` → `social` (smeldr.dev/social v0.8.0). Package decl in all 25 files (21 internal + 4 external `forgesocial_test`→`social_test`), ~120 error/panic/log-string prefixes `forgesocial:`→`social:`, drop alias + `forgesocial.X`→`social.X`, stale cross-refs `forge-mcp`/`forgemcp.X`→`mcp` (A130), social_test.go + router_test.go local var `social`→`svc` (package-name collision), README + CHANGELOG. Preserved: `forge_social_*` DB tables (65 refs, 8 tables — T102), `X-Forge-Signature` (T86/T87), standalone brand words (T101). Gate (forgesocial\|forge-social\|forgemcp\|forge-mcp in *.go) = ZERO. Breaking-MINOR. | Agreed | 2026-06-07 |
| A131 | T100 Step 3: media package rename `forgemedia` → `media` (smeldr.dev/media v1.4.0). Package decl in all 8 files (external `forgemedia_test`→`media_test`), error/panic prefixes `forgemedia:`/`forgemedia.New:`→`media:`/`media.New:` (16), drop alias + `forgemedia.X`→`media.X`, package-doc Forge→Smeldr, stale cross-refs `forge-mcp`/`forgemcp.X`→`mcp` (Step 2), canary fixture renamed, CHANGELOG. Preserved: `forge_media` DB table (10 refs), standalone brand words (T101). v1.3.0→v1.4.0 (T95 → v1.5.0). No exported-symbol/behaviour change. Grep gate literally zero. Independent step. | Agreed | 2026-06-06 |
| A130 | T100 Step 2: mcp package rename `forgemcp` → `mcp` + oauth v0.2.0 adoption (smeldr.dev/mcp v1.17.0). Package decl in all 16 files, godoc selectors, package-doc Forge→Smeldr; dropped `forgeoauth` alias + `forgeoauth.X`→`oauth.X` + dep bump v0.1.5→v0.2.0; `WithOAuth` param `oauth`→`srv` (shadow); stale godoc `forge_format`/`forge_description`→`smeldr_*` (T62/A107); `forge-media`→media, `forge-operator`→operator; README + CHANGELOG. Preserved: `WithForgeFallback`, `forge://` parse-compat, `forge-cli`/`forgemedia.Register` (Step 5/3), standalone brand words. No exported-symbol/behaviour change. Breaking-minor. | Agreed | 2026-06-06 |
| A129 | T100 Step 1: oauth package rename `forgeoauth` → `oauth` (smeldr.dev/oauth v0.2.0). Package decl in 9 production + 2 test files, godoc selectors, error/panic/slog prefixes (`forgeoauth:`/`forge-oauth:` → `oauth:`), README v0.2.0 + `forge-cms.dev` → `smeldr.dev` paths, CHANGELOG. Preserved: `forge_oauth_*` DB tables, `forgemcp` refs (Step 2), test fixtures. No exported-symbol or behaviour change. Breaking-minor; gates Step 2 (mcp). | Agreed | 2026-06-06 |
| A87 | `signals.go`: `AfterSchedule Signal = "after_schedule"` — fires after Scheduled transition, alongside AfterUpdate. Enables `post.scheduled` webhook events and per-signal MCP subscription routing. | Agreed | 2026-05-06 |
| A126 | T04: `ContentTypeStats`, `SiteStats`, `StatsExtProvider`, `App.Stats()`, `App.StatsHandler()`, `App.RegisterStatsProvider()` + `GET /_stats` (Admin). Per-type counts per status; external provider interface for media/other modules. Go 1.26.4 bump (GO-2026-5039, GO-2026-5037). core v1.35.0. | Agreed | 2026-06-04 |
| A128 | T79: in-memory log capture + `GET /_logs` (Admin). `App.CaptureLogs(opts...)`, `LogEntry`, `WithLogCapacity` (default 500), `WithLogLevel` (default WARN); teeing `slog.Handler` (Enabled OR rule so the stderr threshold is never narrowed; `WithAttrs`/`WithGroup` fidelity) over a bounded overwrite-oldest ring (monotonic `seq` + `dropped`). Built-in `*slog.defaultHandler` is substituted with a stderr text handler to avoid a fatal slog/log re-entrancy cycle (`slog.SetDefault` repoints the log package). `GET /_logs` is plain HTTP + bearer (works when MCP is down), envelope `{capacity,count,dropped,entries}` newest-first, query `level`/`limit`/`since`; route absent → 404 when `CaptureLogs` was not called. Ephemeral live-debugging facility (NOT log storage; stderr stays the durable path); HTTP/CLI-only, no MCP tool. core v1.36.0. | Agreed | 2026-06-05 |
| A127 | `smeldr.dev/cli`: `nav` command group (T18). Four Editor-role CLI commands reach full parity with nav MCP tools: `nav list` (table: ID, LABEL, PATH, PARENT, HIDDEN, GHOST, SORT; `--json`), `nav create --label <label> [--path] [--parent-id] [--module] [--hidden] [--ghost] [--sort-order]`, `nav update <id> [same flags]`, `nav delete <id>` (cascades). Closes the last confirmed CLI/MCP gap. cli v0.13.0. | Agreed | 2026-06-04 |
| A125 | T30: `CreateRedirectsTable`, `App.Redirects(db)`, `App.RedirectDB()`, `RedirectStore.Delete` + MCP `create_redirect`/`list_redirects`/`delete_redirect` (Editor+) + CLI `redirect list/create/delete`. Auto-ensure table, no DDL; changes live immediately. core v1.34.0 · mcp v1.16.0 · cli v0.12.0. | Agreed | 2026-06-04 |
| A124 | T53: `NewRateLimiter` + `NewInMemoryCache` — additive constructors returning `(middleware, stopFn)`. Stop function closes an internal channel and blocks until the goroutine confirms exit (`sync.OnceFunc`, idempotent). Existing `RateLimit`/`InMemoryCache` delegate and discard stop — no API breakage. Fixes goroutine leak in tests. core v1.33.0 (minor — new exported symbols). | Agreed | 2026-06-03 |
| A123 | T86 wire-level dual-compat sweep. mcp v1.15.0: resource URIs generated as `smeldr://`; both `smeldr://` and legacy `forge://` accepted on read/subscribe. core v1.32.0: `httpDeliver` dual-emits `X-Smeldr-*` + `X-Forge-*` webhook headers (same values). cli v0.11.0: `SMELDR_URL/TOKEN/MCP_URL` preferred with `FORGE_*` fallback; `init` writes `.smeldr-cli.env`. All changes additive/non-breaking. Legacy `forge://` accept and `X-Forge-*` emit deferred to T87. | Agreed | 2026-06-03 |
| A122 | T88+T89 doc accuracy follow-up: `forge:"required"` → `smeldr:"required"` in live code examples. `README.md` minimal example (2 lines). `core/skills/` full-sync from `common/agent/skills/` — fixes stale struct tags, footer paths, and adds missing sections (SiteConfig, RawHead, block MCP tools, oauth). Enforcement: copilot-instructions M-number doc-gate updated to unconditional `Copy-Item` sync command. Level 1, docs-only, no version bump. | Agreed | 2026-06-02 |
| A121 | T85 core-repo brand sweep: "Forge" → "Smeldr" in all living doc prose and headers across 17 files. Scope: README.md, copilot-instructions.md, CHANGELOG.md (header only), DECISIONS.md (header/intro), ARCHITECTURE.md, REFERENCE.md, FEATURELIST.md, VISION.md (incl. forge-admin → smeldr-admin, Forge Cloud → Smeldr Cloud), SECURITY.md, skills/smeldr.md (+ version line resync), BENCHMARKS.md, CLA.md, Milestone_BACKLOG_TEMPLATE.md, NOTES.md, ERROR_HANDLING.md, example/README.md, example/api/README.md. Preserve: X-Forge-* headers, forge:// URI, FORGE_* env vars, forge-cli binary, historical CHANGELOG/decisions narrative, code identifiers. No version bump. | Agreed | 2026-06-01 |
| A120 | `serveblocks.go`: reference-field resolution (T82). A `{Name}ID` field resolves to a `.{Name}` sub-object = the referenced Published block's `buildData` (`ImageID` → `.Image` with `.MediaURL`/`.AltText`/`.Caption`). `blockFieldFormats.refs` + `refs:["ImageID"]` on content_block/contact_card/hero; `refIDsOf`; one batched `IN()` ref-load pass in `loadTree`; resolve loop in `renderBlock`. `{{ with }}`-guarded (absent/unpublished/dangling → no key), Published-only, one level, no N+1 (counting-DB test). 8 tests. Extends A118; held core v1.31.0. | Agreed | 2026-05-31 |
| A119 | `smeldr.dev/cli`: `block` command group (T32 component 6) mirroring the 12 block MCP tools — `block node create/update/get/list/publish/archive` (Author), `block section`/`block item` `add/reorder/remove` (Editor). One `block` parent verb (architect A4). T77 table output for `node list` (pure `renderTable`/`nodeListTable`; `--json` escape). PascalCase-preserving `--field K=V` + `--fields <json>`. Pure HTTP client via `mcpCall` — no core/mcp import, no go.work. `cliVersion` 0.9.0→0.10.0 (const had lagged shipped tags). `logs` half deferred (A1). 12 tests (pure + httptest mock-MCP). Held with T32. cli v0.10.0. | Agreed | 2026-05-31 |
| A118 | `serveblocks.go` (new): `App.ServeBlocks(dir) (*BlockRenderer, error)` + `BlockRenderer.Render(ctx, pageType, pageID)` — T32 component 4 rendering engine. Assembles a page from blocks + composition edges into HTML via `templates/blocks/<type_name>.html`. Batched load (no N+1), cycle protection (visited-set + maxDepth 16), graceful degradation (unpublished/missing/dangling/malformed/missing-template/exec-error all skip+log, never page-wide). Built-in `blockFieldRegistry` (interim until c7 schema). PascalCase block-`Fields` key convention documented (AGENTS.md). ContentList deferred to c4b. 24 tests. Core engine only — route wiring is convergence. Part of held core v1.31.0. | Agreed | 2026-05-31 |
| A117 | `smeldr.dev/mcp`: block-system generic MCP tools (T32 component 3). `WithBlocks()` server option (constructs `DynamicNode` repo + `ContentEdgeStore` from `Config.DB`). Generic node tier (`node_tools.go`, Author+): `create_node`/`update_node`/`get_node`/`list_nodes`/`publish_node`/`archive_node` — addressed by ID. Composition tier (`edge_tools.go`, Editor+): `add_section`/`reorder_sections`/`remove_section`/`add_item`/`reorder_items`/`remove_item` — distinct names, shared helper, `parent_type`/`child_type` derived. Intercepted before module dispatch; blocks not browsable resources. AGENTS/REFERENCE/FEATURELIST/skill updated. Built vs local core via go.work; held for coordinated core v1.31.0 + mcp v1.14.0 release. | Agreed | 2026-05-31 |
| A116 | `blocks.go`/`edges.go` (new): block-system data foundation (T32 components 1+2). `DynamicNode` (generic block type, `Fields json.RawMessage`), `NewDynamicContentRepo(db)`, `CreateBlockTables(db)` (grouped idempotent creator for `smeldr_dynamic_content` + `smeldr_content_edges` + index; `scheduled_at` added for `SQLRepo` reuse). `ContentEdge`, `ContentEdgeStore`, `NewContentEdgeStore(db)` with `AddChild`/`Children`/`ChildrenOf`(batch)/`RemoveChild`/`Reorder`(atomic CASE); `is_shared` INTEGER↔bool scan. One edge table for page→block + collection→item (T55 D1+D2). Data layer only — MCP/render/seed later. v1.31.0. | Agreed | 2026-05-31 |
| A97 | Audit trail (T21) — `App.Audit(AuditStore)` subscribes to `AfterPublish`, `AfterSchedule`, `AfterArchive`, `AfterDelete` via signal bus; persists `AuditRecord` to SQL. `NewAuditStore(DB)`, `CreateAuditTable(DB)`. GET `/_audit` (Editor+). `forge audit list` CLI. New exported types: `AuditRecord`, `AuditFilter`, `AuditStore`. v1.22.0. | Agreed | 2026-05-16 |
| A98 | Fix data race in `notifyAfter` (`module.go`) — `snapshotItem` takes a shallow reflect copy of `item` before goroutines are spawned; both `dispatchAfter` and `afterHook` goroutine receive the snapshot. Resolves races on G26, G30, G32, G33 detected by `-race`. No exported symbols changed. v1.22.1. | Agreed | 2026-05-19 |
| A99 | Go toolchain upgrade policy — patch: follow within one sprint (govulncheck trigger); minor: within 1–2 months or before Go drops support; go.mod `go` directive tracks latest patch; `toolchain` directive used when patch bump needed but min version stays stable. | Agreed | 2026-05-19 |
| A100 | Go 1.26.3 toolchain bump — `go.mod` `go` directive `go 1.26.2` → `go 1.26.3`. Closes GO-2026-4982, GO-2026-4980, GO-2026-4971, GO-2026-4918. CI auto-picks version via `go-version-file: go.mod`. v1.22.2. | Agreed | 2026-05-19 |
| A101 | `SingleInstance()` and `Standalone()` module routing options. `SingleInstance`: serves first Published item at `GET /{prefix}`; slug URLs not registered. `Standalone`: App dispatches `GET /{slug}` top-level across all Standalone modules; `GET /{prefix}/{slug}` not registered; list at `GET /{prefix}` retained. `MCPMeta.SingleInstance bool` added; `forge-mcp` suppresses `list_{type}s` for SingleInstance modules. v1.23.0. | Agreed | 2026-05-23 |
| D32 | decisions/ file system restructure — flat role-separated system with rolling working file (`recent.md`), Non-Decisions file (`nondecisions.md`), phase2.md archived as `phase2-archive.md`. Archiving is architect-directed at ~20KB. | Active | 2026-05-17 |

### Non-Decisions — [decisions/nondecisions.md](decisions/nondecisions.md)

| # | Title | Status | Date |
|---|-------|--------|------|
| A96 | Non-Decision: sitemap ping (T39) — Forge will not provide opt-in sitemap ping. Google deprecated their endpoint in 2023; IndexNow requires API key + verification file (app-level setup). Developer pattern: `App.OnSignal(AfterPublish, ...)`. REFERENCE.md: new "Search engine indexing" section. | Agreed | 2026-05-16 |
| ND-T104 | Non-Decision: dynamic content slug immutability (T104) — `UpdateFields` will not regenerate the slug when the title field changes. URL stability is a first-class SEO requirement; slug mutation without operator intent violates least-surprise. Operators can write slug directly via `UpdateFields({Slug: "new-slug"})`. | Agreed | 2026-06-16 |
| A156 | Non-Decision: HTML rendering for dynamic types + cloud block rendering (A156a, A156b) — Core will not provide HTML rendering for dynamic content types or supply block templates for cloud context. Rendering is a presentation concern (cloud layer); core is data/lifecycle. JSON API (`GET /{prefix}/{slug}`) serves headless by default. Standalone sites use own templates + JSON. Cloud operators use presentation layer templates. | Agreed | 2026-06-17 |
| A172 | Non-Decision: BSL and SSPL declined — Core will not relicense from AGPL to BSL or SSPL. Both restrict production use, contradicting Article XII (Ownership). Smeldr started protective; the relicense escape is not needed. See `constitution/cloud-strategy.md`. | Declined | 2026-06-28 |

---

> **Body text:** D1–D22, A19–A65, A87, A88–A95, A97–A101 → [`decisions/core.md`](decisions/core.md) · D25, A66, D26, A83 → [`decisions/auth.md`](decisions/auth.md) · D27, A67, A74, A75, A77 → [`decisions/content-api.md`](decisions/content-api.md) · D28, D32, A69–A72, A76, A84–A86 → [`decisions/docs.md`](decisions/docs.md) · A73, D31, A79 → [`decisions/media.md`](decisions/media.md) · D29, D30, A82 → [`decisions/nav.md`](decisions/nav.md) · A68, A78, A80, A81 → [`decisions/storage.md`](decisions/storage.md) · A102–A120 → [`decisions/phase3-archive.md`](decisions/phase3-archive.md) · A121–A125 → [`decisions/phase4-archive.md`](decisions/phase4-archive.md) · A126–A130 → [`decisions/phase5-archive.md`](decisions/phase5-archive.md) · A131–A135 → [`decisions/phase6-archive.md`](decisions/phase6-archive.md) · A136–A138 → [`decisions/phase7-archive.md`](decisions/phase7-archive.md) · A139–A150 → [`decisions/phase8-archive.md`](decisions/phase8-archive.md) · A151–A157 → [`decisions/phase9-archive.md`](decisions/phase9-archive.md) · A158–A169 → [`decisions/phase10-archive.md`](decisions/phase10-archive.md) · A170–A183 → [`decisions/phase11-archive.md`](decisions/phase11-archive.md) · A184–A190 → [`decisions/phase12-archive.md`](decisions/phase12-archive.md) · A191–A193 → [`decisions/phase13-archive.md`](decisions/phase13-archive.md) · A194–A200 → [`decisions/phase14-archive.md`](decisions/phase14-archive.md) · A201–A209 → [`decisions/phase15-archive.md`](decisions/phase15-archive.md) · A210–A214 → [`decisions/phase16-archive.md`](decisions/phase16-archive.md) · A96, ND-T104, A156, A172 → [`decisions/nondecisions.md`](decisions/nondecisions.md) · phase2-archive.md — superseded; use topic files above
